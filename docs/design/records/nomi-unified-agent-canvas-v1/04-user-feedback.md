# User feedback · v1

Status: awaiting user review.

The user clarified that the visual workflow must be:

```text
真实 Nomi 页面参考图
  → 结合当前代码/设计系统/需求写 Prompt Brief
  → 使用内置图片生成能力生成 UI exploration
  → 用户核对结构、层级、密度和下一步
  → 只有确认后才冻结 design direction
```

The attached canvas onboarding image is a reference for the pattern “tell the user where to start and let them complete tasks one by one”; it is not a request to clone that product’s visual design.

No design direction is confirmed yet.

## v1 样张已生成

- 输出：`03-image2/v1-exploration.png`
- 生成方式：Codex 内置图片生成
- 参考输入：Nomi 真实 Agent、生成画布和工作台引导截图
- 本版只改变：`workspace-composition-and-information-hierarchy`
- 需要用户重点确认：固定左栏、中央画布与右侧 Agent 的比例；结果的收纳方式；视频拆解表在画布中的可读性；Skill 引导卡是否过重；是否需要补一张全屏 Agent 状态样张。
