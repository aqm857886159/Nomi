# Agent UI 异常态覆盖缺口清单

> 生成时间：2026-09-02T17:28:01.827Z
> 来源：扫描 21 形态（`2026-09-01-agent-ui-final-redesign.md`）× 5 种异常态
> **本文只列缺口，不补设计**——补画是设计班的工作，须过样张门（R8）。

## 汇总

| 级别 | 组合数 | 必须补画数 |
|---|---|---|
| **P0**（用户必然遇到） | 17 | 17 |
| P1（用户可能遇到） | 46 | 42 |
| P2（边缘情形） | 6 | 0 |
| **合计缺口** | **69** | **59** |
| 已覆盖 | 36 | — |
| 总计（21形态×5态） | 105 | — |

## P0 缺口（必须补画 · 设计班优先处理）

| 形态 | 异常态 | 缺口描述 | 必须补画 |
|---|---|---|---|
| 3·用户气泡 | 超长文本 | 超长提示词（>500字）气泡折叠策略未画——折叠后展开，还是滚动，还是截断？ | **是** |
| 6·工具条 | 超长文本 | 工具数量很多（>20 步）时总览行文案策略（「20 步 ·...」 vs 「20+ 步」？点开是否分组？） | **是** |
| 7·普通回复 | 超长文本 | 超长回复（>1000字/多段落/含代码块）折叠策略——「展开全文」 vs 全部展开？Markdown 渲染边界？ | **是** |
| 8·计划卡 | 加载中 | 生成计划中（AI 还在拆镜头）时计划卡的骨架态未画——用户能看到「正在生成计划…」的中间态？ | **是** |
| 8·计划卡 | 错误 | 计划生成失败（AI 未能拆出合法计划）的失败态卡未画 | **是** |
| 8·计划卡 | 超长文本 | 计划 >10 镜时卡内列表的高度封顶策略（卡内滚动 vs 折叠 vs 分页？）——这是 Nomi 独特卡片的高频边界 | **是** |
| 9·付费确认卡 | 错误 | 价格计算失败时的卡态——是否允许重试？是否显示估算价格？ | **是** |
| 10·写入回执 | 错误 | 写入画布失败（节点无法创建）的错误回执未画——红色错误行？重试？ | **是** |
| 11·排队行 | 超长文本 | 多条排队（>3 条）时排队区高度上限——是否折叠/计数？贴 composer 的约束还在吗？ | **是** |
| 13·产物缩略卡 | 加载中 | 图像正在生成时的卡状态——是否有进度渐显？骨架？待生成提示？ | **是** |
| 13·产物缩略卡 | 错误 | 图像生成失败的产物卡未画——是显示红边错误态还是流向形态 15 失败卡？ | **是** |
| 14·多候选组 | 超长文本 | 候选数 >3 时的折叠策略（testspec 说「多于 3 版折叠」但样张只画了 3 版并排） | **是** |
| 16·有出入卡 | 超长文本 | 出入点很多（>5 处不一致）时的列表高度封顶（卡内滚动？前 N 条 + 「另有 M 处」？） | **是** |
| 17·反问卡 | 超长文本 | 问题文字很长（>100字）或选项很多（>5）时的高度/折叠策略 | **是** |
| 18·指令队列 | 超长文本 | 同形态 11（>3 条排队） | **是** |
| 19·@ 选择器 | 空状态 | 项目无任何素材/技能时 @ 选择器的空状态未画（「暂无可引用内容」？引导上传？） | **是** |
| 19·@ 选择器 | 超长文本 | 素材 >50 时的分组/搜索策略未画（滚动区高度上限？搜索框是否常驻？） | **是** |

## P1 缺口（应该覆盖 · 可随实现迭代补齐）

| 形态 | 异常态 | 缺口描述 | 必须补画 |
|---|---|---|---|
| 1·上下文用量 | 空状态 | 新会话 0 轮时的初始态未画（环形图 0% + 「还能聊 ~N 轮」是否显示 "首轮"？） | 是 |
| 1·上下文用量 | 加载中 | 首次请求中用量尚不确定态——可以隐藏 pill 或显示「—」 | 否 |
| 2·压缩分隔线 | 超长文本 | 折叠 N 轮中 N 很大（如 999）时的数字显示——是否截断？ | 是 |
| 3·用户气泡 | 错误 | 消息发送失败时气泡的错误态（红边？重试按钮？） | 是 |
| 3·用户气泡 | 最小窗 | 最小窗 340px 宽时气泡最大宽度边界——占比多少？文字是否换行？ | 是 |
| 4·思考条 | 错误 | 思考过程中断/超时的降级态未画（是否显示「思考中断」？落定为错误句？） | 是 |
| 4·思考条 | 超长文本 | 思考时间超长（>60s）时计时显示格式（01:23 vs 83s？） | 是 |
| 5·阶段分隔线 | 加载中 | 工序图示动效资产加载失败的降级（SVG 近似 vs 纯文字） | 是 |
| 5·阶段分隔线 | 最小窗 | 最小窗 340px 时工序图示 + 文字是否能完整显示？还是图示压缩/文字折行？ | 是 |
| 6·工具条 | 错误 | 单个工具调用失败时该行的状态（红色 × ？还是跳过？） | 是 |
| 6·工具条 | 最小窗 | 最小窗时工具行摘要文字截断策略（「6 步 · 读剧本 · 拆...」折断位） | 是 |
| 7·普通回复 | 错误 | 回复被截断/中断时的行尾标记（「⚠ 回复中断」？） | 是 |
| 8·计划卡 | 空状态 | 0 镜计划卡（所有镜头被过滤掉）的空态 CTA 未画 | 是 |
| 8·计划卡 | 最小窗 | 最小窗 340px 时计划卡的参数药丸是否换行？按钮组是否折叠？ | 是 |
| 9·付费确认卡 | 加载中 | 价格计算中（从服务端拉取）的骨架态——显示「计算中…」还是先禁用「确认」按钮？ | 是 |
| 9·付费确认卡 | 超长文本 | 很多镜头（>10）时明细行列表高度封顶（卡内滚 vs 折叠） | 是 |
| 9·付费确认卡 | 最小窗 | 最小窗时「确认并生成 ¥X.XX」按钮文字是否截断？ | 是 |
| 10·写入回执 | 加载中 | 「正在写入画布…」的中间态——是否显示 loading 行，还是写完了才出回执？ | 是 |
| 10·写入回执 | 超长文本 | 「已加 99 个节点」时回执行是否换行？数字是否有上限（如 99+）？ | 是 |
| 10·写入回执 | 最小窗 | 回执行单行设计，略窄可换行 | 否 |
| 11·排队行 | 错误 | 排队消息在等待期间模型失联时，是否仍保留排队行还是清空？ | 是 |
| 11·排队行 | 最小窗 | 最小窗时多条排队行的布局（qdot + 文字 + × 在 340px 宽下的截断点） | 是 |
| 12·进行中行 | 超长文本 | 人话文字很长（>60字）时工具行摘要换行策略 | 是 |
| 12·进行中行 | 最小窗 | 最小窗时「转圈 + 文字 + 计时 + ▾」一行在 340px 下的排列（文字是否截断？） | 是 |
| 13·产物缩略卡 | 空状态 | 图像生成中（卡已出现但图尚未生成）的骨架缩略图态未画 | 是 |
| 13·产物缩略卡 | 超长文本 | 标题/描述超长时的截断策略（「镜 1 特写 · 1024×1792 · 第 2 版」在 340px 下） | 是 |
| 13·产物缩略卡 | 最小窗 | 最小窗时缩略图高度是否缩减？标题行是否换行？ | 是 |
| 14·多候选组 | 空状态 | 0 候选时卡是否出现？（不应出现，但后端可能返回空数组） | 是 |
| 14·多候选组 | 加载中 | 候选生成中（某些候选已完成某些还在生成）的混合态未画 | 是 |
| 14·多候选组 | 错误 | 部分候选失败时的卡态（「A 版：失败 · B 版：成功」的混合展示） | 是 |
| 14·多候选组 | 最小窗 | 最小窗时多候选并排布局——是否变单列？ | 是 |
| 15·失败卡 | 超长文本 | 错误信息很长时的文字处理（折叠/展开？） | 是 |
| 15·失败卡 | 最小窗 | 最小窗时「换模型重试 · 改提示词 · 看详情」三按钮的布局（是否换行/折叠） | 是 |
| 16·有出入卡 | 错误 | 有出入卡里「让 AI 修」再次失败时的第二层错误态 | 是 |
| 16·有出入卡 | 最小窗 | 最小窗时三按钮+出入列表的布局 | 是 |
| 17·反问卡 | 空状态 | 0 个选项时的卡态（AI 问了问题但没给选项，只有输入框？） | 是 |
| 17·反问卡 | 加载中 | 生成问题中的骨架态未画 | 是 |
| 17·反问卡 | 错误 | 用户答完但 AI 处理失败时的反馈 | 是 |
| 17·反问卡 | 最小窗 | 最小窗时问题文字 + 选项按钮的布局 | 是 |
| 18·指令队列 | 错误 | 同形态 11 | 否 |
| 18·指令队列 | 最小窗 | 同形态 11 | 否 |
| 19·@ 选择器 | 加载中 | 素材库加载中时选择器的骨架态 | 是 |
| 19·@ 选择器 | 错误 | at 选择器加载失败时的错误态 | 是 |
| 19·@ 选择器 | 最小窗 | 最小窗时选择器弹窗的最大高度和位置（避免超出 720px 视口） | 是 |
| 20·技能载入行 | 错误 | 技能载入失败时的事件行（红字警告？静默？还是 fallback 默认行为？） | 是 |
| 20·技能载入行 | 超长文本 | 技能名很长（>20字）时的截断策略 | 是 |

## P2 缺口（可延后 · 开闸前覆盖即可）

| 形态 | 异常态 | 缺口描述 |
|---|---|---|
| 1·上下文用量 | 错误 | 用量 API 失败时的降级（静默隐藏 vs 错误标记） |
| 2·压缩分隔线 | 错误 | 展开历史失败时的提示（可 toast，无需专项设计） |
| 2·压缩分隔线 | 最小窗 | 分隔线宽度随面板，无特殊断点 |
| 4·思考条 | 最小窗 | 单行设计，宽度随面板自适应 |
| 7·普通回复 | 最小窗 | 文字自动换行，无特殊断点 |
| 20·技能载入行 | 最小窗 | 单行，自适应 |

## 已覆盖的异常态组合

| 形态 | 异常态 | 覆盖情况 |
|---|---|---|
| 1·上下文用量 | 超长文本 | ✓（接近上限时 warning 色文档已描述，但样张未画） |
| 1·上下文用量 | 最小窗 | ✓（pill 自身足够小，挤压由 flex 处理，无需专项设计） |
| 2·压缩分隔线 | 空状态 | ✓（不存在空态——只有历史时才出现） |
| 2·压缩分隔线 | 加载中 | ✓（加载历史时分隔线本就不显示） |
| 3·用户气泡 | 空状态 | ✓（不存在空态——有消息才有气泡） |
| 3·用户气泡 | 加载中 | ✓（消息发出后立即有气泡，无等待态） |
| 4·思考条 | 空状态 | ✓（不存在空态） |
| 4·思考条 | 加载中 | ✓（进行中态样张已画（转圈 + 计时）） |
| 5·阶段分隔线 | 空状态 | ✓（不存在空态） |
| 5·阶段分隔线 | 错误 | ✓（分隔线本身无错误态） |
| 5·阶段分隔线 | 超长文本 | ✓（文字内联，宽度自适应） |
| 6·工具条 | 空状态 | ✓（无工具调用时不显示该行） |
| 6·工具条 | 加载中 | ✓（进行中态样张已画（转圈 + 计时 + ▾）） |
| 7·普通回复 | 空状态 | ✓（不存在空回复（有 reply 节点必有文字）） |
| 7·普通回复 | 加载中 | ✓（流式扫光光标样张已画） |
| 9·付费确认卡 | 空状态 | ✓（不存在空态（有卡必有明细）） |
| 10·写入回执 | 空状态 | ✓（不存在空态） |
| 11·排队行 | 空状态 | ✓（空闲时不显示排队区） |
| 11·排队行 | 加载中 | ✓（忙时排队态样张已画） |
| 12·进行中行 | 空状态 | ✓（不存在空态） |
| 12·进行中行 | 加载中 | ✓（进行中态样张已画） |
| 12·进行中行 | 错误 | ✓（失败态流向形态 15 失败卡） |
| 15·失败卡 | 空状态 | ✓（不存在空态） |
| 15·失败卡 | 加载中 | ✓（失败已发生，无 loading 中间态） |
| 15·失败卡 | 错误 | ✓（失败态样张已画） |
| 16·有出入卡 | 空状态 | ✓（全部对上时不出现卡（testspec B-03 已明确）） |
| 16·有出入卡 | 加载中 | ✓（对账在 AI 侧，用户等待时看的是进行中行） |
| 18·指令队列 | 空状态 | ✓（同形态 11） |
| 18·指令队列 | 加载中 | ✓（同形态 11） |
| 20·技能载入行 | 空状态 | ✓（无技能载入时不显示该行） |
| 20·技能载入行 | 加载中 | ✓（载入中可以不显示（载完才显）） |
| 21·常驻技能标记 | 空状态 | ✓（v3 已删除，无需覆盖） |
| 21·常驻技能标记 | 加载中 | ✓（v3 已删除，无需覆盖） |
| 21·常驻技能标记 | 错误 | ✓（v3 已删除，无需覆盖） |
| 21·常驻技能标记 | 超长文本 | ✓（v3 已删除，无需覆盖） |
| 21·常驻技能标记 | 最小窗 | ✓（v3 已删除，无需覆盖） |

---

## 行动建议（设计班）

### 立刻补画（P0 · 17 个）
- **形态 3·用户气泡 × 超长文本**：超长提示词（>500字）气泡折叠策略未画——折叠后展开，还是滚动，还是截断？
- **形态 6·工具条 × 超长文本**：工具数量很多（>20 步）时总览行文案策略（「20 步 ·...」 vs 「20+ 步」？点开是否分组？）
- **形态 7·普通回复 × 超长文本**：超长回复（>1000字/多段落/含代码块）折叠策略——「展开全文」 vs 全部展开？Markdown 渲染边界？
- **形态 8·计划卡 × 加载中**：生成计划中（AI 还在拆镜头）时计划卡的骨架态未画——用户能看到「正在生成计划…」的中间态？
- **形态 8·计划卡 × 错误**：计划生成失败（AI 未能拆出合法计划）的失败态卡未画
- **形态 8·计划卡 × 超长文本**：计划 >10 镜时卡内列表的高度封顶策略（卡内滚动 vs 折叠 vs 分页？）——这是 Nomi 独特卡片的高频边界
- **形态 9·付费确认卡 × 错误**：价格计算失败时的卡态——是否允许重试？是否显示估算价格？
- **形态 10·写入回执 × 错误**：写入画布失败（节点无法创建）的错误回执未画——红色错误行？重试？
- **形态 11·排队行 × 超长文本**：多条排队（>3 条）时排队区高度上限——是否折叠/计数？贴 composer 的约束还在吗？
- **形态 13·产物缩略卡 × 加载中**：图像正在生成时的卡状态——是否有进度渐显？骨架？待生成提示？
- **形态 13·产物缩略卡 × 错误**：图像生成失败的产物卡未画——是显示红边错误态还是流向形态 15 失败卡？
- **形态 14·多候选组 × 超长文本**：候选数 >3 时的折叠策略（testspec 说「多于 3 版折叠」但样张只画了 3 版并排）
- **形态 16·有出入卡 × 超长文本**：出入点很多（>5 处不一致）时的列表高度封顶（卡内滚动？前 N 条 + 「另有 M 处」？）
- **形态 17·反问卡 × 超长文本**：问题文字很长（>100字）或选项很多（>5）时的高度/折叠策略
- **形态 18·指令队列 × 超长文本**：同形态 11（>3 条排队）
- **形态 19·@ 选择器 × 空状态**：项目无任何素材/技能时 @ 选择器的空状态未画（「暂无可引用内容」？引导上传？）
- **形态 19·@ 选择器 × 超长文本**：素材 >50 时的分组/搜索策略未画（滚动区高度上限？搜索框是否常驻？）

### 本轮迭代补画（P1 · 42 个）
- 形态 1·上下文用量 × 空状态：新会话 0 轮时的初始态未画（环形图 0% + 「还能聊 ~N 轮」是否显示 "首轮"？）
- 形态 2·压缩分隔线 × 超长文本：折叠 N 轮中 N 很大（如 999）时的数字显示——是否截断？
- 形态 3·用户气泡 × 错误：消息发送失败时气泡的错误态（红边？重试按钮？）
- 形态 3·用户气泡 × 最小窗：最小窗 340px 宽时气泡最大宽度边界——占比多少？文字是否换行？
- 形态 4·思考条 × 错误：思考过程中断/超时的降级态未画（是否显示「思考中断」？落定为错误句？）
- 形态 4·思考条 × 超长文本：思考时间超长（>60s）时计时显示格式（01:23 vs 83s？）
- 形态 5·阶段分隔线 × 加载中：工序图示动效资产加载失败的降级（SVG 近似 vs 纯文字）
- 形态 5·阶段分隔线 × 最小窗：最小窗 340px 时工序图示 + 文字是否能完整显示？还是图示压缩/文字折行？
- 形态 6·工具条 × 错误：单个工具调用失败时该行的状态（红色 × ？还是跳过？）
- 形态 6·工具条 × 最小窗：最小窗时工具行摘要文字截断策略（「6 步 · 读剧本 · 拆...」折断位）
- 形态 7·普通回复 × 错误：回复被截断/中断时的行尾标记（「⚠ 回复中断」？）
- 形态 8·计划卡 × 空状态：0 镜计划卡（所有镜头被过滤掉）的空态 CTA 未画
- 形态 8·计划卡 × 最小窗：最小窗 340px 时计划卡的参数药丸是否换行？按钮组是否折叠？
- 形态 9·付费确认卡 × 加载中：价格计算中（从服务端拉取）的骨架态——显示「计算中…」还是先禁用「确认」按钮？
- 形态 9·付费确认卡 × 超长文本：很多镜头（>10）时明细行列表高度封顶（卡内滚 vs 折叠）
- 形态 9·付费确认卡 × 最小窗：最小窗时「确认并生成 ¥X.XX」按钮文字是否截断？
- 形态 10·写入回执 × 加载中：「正在写入画布…」的中间态——是否显示 loading 行，还是写完了才出回执？
- 形态 10·写入回执 × 超长文本：「已加 99 个节点」时回执行是否换行？数字是否有上限（如 99+）？
- 形态 11·排队行 × 错误：排队消息在等待期间模型失联时，是否仍保留排队行还是清空？
- 形态 11·排队行 × 最小窗：最小窗时多条排队行的布局（qdot + 文字 + × 在 340px 宽下的截断点）
- 形态 12·进行中行 × 超长文本：人话文字很长（>60字）时工具行摘要换行策略
- 形态 12·进行中行 × 最小窗：最小窗时「转圈 + 文字 + 计时 + ▾」一行在 340px 下的排列（文字是否截断？）
- 形态 13·产物缩略卡 × 空状态：图像生成中（卡已出现但图尚未生成）的骨架缩略图态未画
- 形态 13·产物缩略卡 × 超长文本：标题/描述超长时的截断策略（「镜 1 特写 · 1024×1792 · 第 2 版」在 340px 下）
- 形态 13·产物缩略卡 × 最小窗：最小窗时缩略图高度是否缩减？标题行是否换行？
- 形态 14·多候选组 × 空状态：0 候选时卡是否出现？（不应出现，但后端可能返回空数组）
- 形态 14·多候选组 × 加载中：候选生成中（某些候选已完成某些还在生成）的混合态未画
- 形态 14·多候选组 × 错误：部分候选失败时的卡态（「A 版：失败 · B 版：成功」的混合展示）
- 形态 14·多候选组 × 最小窗：最小窗时多候选并排布局——是否变单列？
- 形态 15·失败卡 × 超长文本：错误信息很长时的文字处理（折叠/展开？）
- 形态 15·失败卡 × 最小窗：最小窗时「换模型重试 · 改提示词 · 看详情」三按钮的布局（是否换行/折叠）
- 形态 16·有出入卡 × 错误：有出入卡里「让 AI 修」再次失败时的第二层错误态
- 形态 16·有出入卡 × 最小窗：最小窗时三按钮+出入列表的布局
- 形态 17·反问卡 × 空状态：0 个选项时的卡态（AI 问了问题但没给选项，只有输入框？）
- 形态 17·反问卡 × 加载中：生成问题中的骨架态未画
- 形态 17·反问卡 × 错误：用户答完但 AI 处理失败时的反馈
- 形态 17·反问卡 × 最小窗：最小窗时问题文字 + 选项按钮的布局
- 形态 19·@ 选择器 × 加载中：素材库加载中时选择器的骨架态
- 形态 19·@ 选择器 × 错误：at 选择器加载失败时的错误态
- 形态 19·@ 选择器 × 最小窗：最小窗时选择器弹窗的最大高度和位置（避免超出 720px 视口）
- 形态 20·技能载入行 × 错误：技能载入失败时的事件行（红字警告？静默？还是 fallback 默认行为？）
- 形态 20·技能载入行 × 超长文本：技能名很长（>20字）时的截断策略

### 不需要补画（实现者自行决策）
- 形态 1·上下文用量 × 加载中（P1）：首次请求中用量尚不确定态——可以隐藏 pill 或显示「—」
- 形态 10·写入回执 × 最小窗（P1）：回执行单行设计，略窄可换行
- 形态 18·指令队列 × 错误（P1）：同形态 11
- 形态 18·指令队列 × 最小窗（P1）：同形态 11
- 形态 1·上下文用量 × 错误（P2）：用量 API 失败时的降级（静默隐藏 vs 错误标记）
- 形态 2·压缩分隔线 × 错误（P2）：展开历史失败时的提示（可 toast，无需专项设计）
- 形态 2·压缩分隔线 × 最小窗（P2）：分隔线宽度随面板，无特殊断点
- 形态 4·思考条 × 最小窗（P2）：单行设计，宽度随面板自适应
- 形态 7·普通回复 × 最小窗（P2）：文字自动换行，无特殊断点
- 形态 20·技能载入行 × 最小窗（P2）：单行，自适应