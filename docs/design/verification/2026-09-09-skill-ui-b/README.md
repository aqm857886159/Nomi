# skill-ui-b：同类变体与封面验收

状态：已完成。最终 pnpm run gates 在 aa94da24305b 上 exit 0；冷启动折叠、卡片引用/撤销和88张媒体验收通过。

真实来源：内置表情包25项（五种情绪各五档），不是四个策展技能正文里的配方展开。开工截图 `before-composer.png`、`before-library.png`；搜索喜悦时原本仍是五项平铺。

## 规则与真实任务

| 面 | 本次实现 | 验收证据 |
|---|---|---|
| Composer | 明确类别超过4项默认收一行；展开后可选任意项 | 01/02，真实25项 |
| 技能卡片库 | 同类别卡片成组、超过4项默认折叠；保留搜索和详情应用 | 03/04；选喜悦第一档成功写入节点 |
| 提示词库 | 同规则驱动虚拟行；类别摘要与卡片行分别估算高度 | 05/06；展开/收起及第一张媒体可见 |
| 节点更多 | 共享类别投影，超过4项时菜单内展开，不关闭菜单 | 07；菜单沿用原坐标边界 |
| 12项夹具 | 默认一行，Enter展开12项，选第12项，再收起 | fixture-12 / fixture-12-expanded |

HTML 可视规格：[真实宿主折叠样张](folding-specimen.html)。本任务书已指定折叠形态，样张据真实运行结果呈现；没有冒称本会话另获用户截图批准，也没有更新 design-lab 基线。

## 红绿与边界

- 旧构建运行同一25项折叠断言，找不到摘要而失败：`/private/tmp/nomi-skill-ui-b-ui-red.log`；新实现运行真实Electron成功：`journey.json`。
- 共享分组12项/4-5阈值/跨类别夹具先红后绿；`libraryGroups.test.ts`。
- 原许可解析器拒绝本仓AGPL-3.0-only，夹具先红后绿；保留缺许可和越界路径拒绝测试。
- 88技能全有真实封面夹具先红后绿，48个技能显式工作台展示夹具先红后绿；既有55媒体不重复生成。
- 收据使用provider task cost核算本任务费用；共享账户差额另存。第五张差额包含额外账户支出，未冒称全部属于本任务。
- 本任务没有修改技能正文：所有88份与整合后的1e6bcc85c逐字节相同（storyboard正文仅随上游PR #681更新）。33份只加frontmatter的标准metadata扩展与本仓原许可，不改已有工具和阶段声明。

截图包含真实桌面外壳；自动Feel报告对隐藏折叠内容、浮层背后内容和刻意省略也会报候选，人眼按可见交互判定。没有降低门岗、增加视觉豁免或改基线。

## 全部封面真机验收

`cover-wall.json`逐条记录48技能+40效果图片加载成功；`wall-skill-*.png`和`wall-effect-*.png`是实际滚动后的桌面截图，非拼接模拟。每张均滚动到卡片、展开所属组、检查真实协议图片解码成功。

本次生成33+返工3=36次，采用33张；3张首轮不合格保留不用。费用$0.306，按8元/美元保守折算¥2.448。详情见`../../covers/skill-ui-b-visual-review.md`、88项接触表和`generation-receipt.json`。

完整验证：76门岗、73通过、0阻断失败、3文档advisory；1331个测试文件通过，12272项测试通过、2项跳过；Vite与Electron构建通过。原始完整日志：`/private/tmp/nomi-skill-ui-b-full-gates-5.log`。

冷启动补充：内置表情曾被外部源下载阻塞；cold-start-red/green.txt证明未完成网络也能立即返回25项表情，旧磁盘缓存立即返回且后台更新可在下次读取。共享入口仅复用既有缓存与刷新器。

补充门岗核查：一次完整视觉检查中既有sb-zone-10-playback-skipped发生63像素SVG字形差异；未改代码/基线/容差，定向连续3次通过，记录见playback-recheck.txt。之后完整gates重新执行。

最终完整收据：`/private/tmp/nomi-skill-ui-b-full-gates-7.log`；76门岗0阻断失败、3文档advisory，1332测试文件/12274测试通过、2项跳过，Vite/Electron构建通过。先前SVG文字差异用例本轮完整视觉检查通过，未更新基线或容差。
