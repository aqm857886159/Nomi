# Agent 面板整理样张（2026-09-25）

在线画布：https://claude.ai/artifact/PwZiq9tRTbiZukvAHZ4J17（可编辑：页面顶部「改文案」直接改画板上的字）。
本目录是离线留存：`index.html` 由 `build.mjs` 生成（图标从 `@tabler/icons-react` 抽真实路径，颜色逐字取自 `src/theme/nomi-tokens.css`，改前截图取自 `docs/evidence/2026-09-25-agent-panel-tidy/*-before-*.png`）；`preview/` 是亮 / 暗两套渲染图。
对应 PR #869。五条决定（D1–D5）拍板前，PR 代码不按样张改；拍板后按样张逐项对账。
