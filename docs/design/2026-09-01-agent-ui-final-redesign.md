# Agent 界面最终版重做设计 —— 样张裁决包（纯设计）

> 日期：2026-09-01 · **v3 修订 2026-09-02**（v2 同日在前）· 状态：📋 设计定稿提案 + 可体验样张，**样张门待用户拍板**（拍板前不写生产代码）
> **v3 = 照用户 2026-09-02 凌晨九条整改令 + 总纲重排**。总纲（最高效力）：**agent 要长得像「住在对话里的 Claude Code」——一切事件进对话流，常驻栏越薄越好**。九条落账：① 重复的技能载入区删净（只留对话流一处）；② 技能加载 = 对话流一行灰字小事件，常驻「技能已装载」区删除 → **#194 形态 19/21 的「持续可见」要求被本总纲推翻**（19/20/21 三形态合并为形态 20 的事件行，实现前需同步改 `nomi-agent-interaction.md` §4）；③ 参考图/定妆走 composer `@`（与引用素材同一手势），对话外引用架/专用槽删除（形态 19 的架随之取消）；④ 排队 = 消息排队：浅色行贴输入框上沿、× 撤回、到点自动进对话，v2 任务队列卡删除（形态 18 改形）；⑤ 工具 = 一行 + 行尾 ▾ 折叠（进行中当前一行、结束收拢一行总览），不占独立横条不做面板区块（形态 6 改形）；⑥ **词汇闸**：UI 上零内部术语（「id/原位/过闸/校验分/阈值/日志」等全改人话）；⑦ 动作按钮 icon 化（撤销 → ↩ + tooltip；决定类按钮保留文字）；⑧ 删 chrome 废信息（agent 头部项目名、「当前现场」、「结果卡区」标签）；⑨ 认知负荷复审：屏 A 常驻可点 19→10 · 屏 B 卡均 4.3→4.0（卡内注释 5→0）· 屏 D 12→10。**逐屏逐件人话走读见 `2026-09-02-agent-ui-v3-walkthrough.md`（编排者拿它转述）。** 另两处随⑥/D4 收紧：付费卡 agent 档**无「本会话不再提示」开关也无倒计时**（倒计时只属外部 MCP 档，D4 建议直接落样张）；计划卡按钮改「生成已选（N 镜）」。
> **v3.2 = 照用户 2026-09-02 布局拍板补齐布局设计**：① 面板在**创作/分镜/生成/预览 + 浏览器打开时**的逐面落位（一个家=视口右缘、一个宽度=`assistantWidth`、让位不悬浮）；② 缩小三档（收起=顶栏角标+蓝点 · 窄窗=只收上限不偷改 · 最小窗 1100×720 实数）；③ 面板操控三能力（左缘拖宽 8px 热区 300–600 · 放大=一键到上限不盖画布 · 拖走=浮动窗、换边否决）。样张新增「屏 E · 布局」页签（静态标注），规格数字全在 **§11**，人话走读在 walkthrough「屏 E」一节。
> **v3.1 = 照用户 2026-09-02 两条快修裁决**（「结果卡太大了」「聊天下面放 icon 吧，太多字了」）：**① 屏 D 固定结果卡默认收成一根细条**「📌 拆解结果 · N 镜 · 已选 N ▾」（高度≈一条工具行；状态摘要常在、随勾选实时变；点开全表封顶约视口 45% 表内滚动，再点收回；收起只是收起不清空；屏 C 节点浮条/角标语义不变）；**② composer 底排全 icon 化**（📎⚡🤖✏️ 去文字只留 Tabler 图标，hover tooltip 给完整含义「Agent · 安全自动」「文本模型 · Kimi K2」；模型未选 = 🤖 右上角小红点 + tooltip「去选文本模型」，替掉常驻文字提示——C4 的 v3.1 形态；↑ 发送不变）。负荷账：屏 A 常驻可点仍 10、底排常驻文字 2 句→0；屏 D 常驻可点 10→**9**。
> **v2 = 照用户 2026-09-02 四条总方向重做**：① 组件**整件复用**成熟库——Beautiful UI（beautifului.dev，用户偏向）优先、AI Elements（elements.ai-sdk.dev）次之，我们只做设计系统适配（尺寸/Tabler icon/token）；② 两库覆盖不了的**独有件走竞品还原**（证据 file:line，禁脑补）；③ **最高原则=认知负荷最低**（每屏数可见选择数并压到最少，取舍冲突以此裁）；④ 反馈动效要有但**极简披露**。21 形态逐个过四步流水线（分类→找证据→还原解剖→套设计系统+负荷审计），映射表见 §4.0。
> 分支：`docs/agent-ui-redesign-20260901`（从 `origin/main@f9107740` 起）
> 前提：用户裁定当前 `ProjectAgentResidentShell`（742 行）只是**过渡载具**，最终交互**必须按资料重新设计**、另过样张门才开闸（`RESIDENT-SHELL-DECISION.md` 的「B 档带条件收」）。
> 本包 = 那道样张门的裁决物：把过渡版的现状、最终版的目标态、逐条改动理由（引用来源）落成单一文档，配 5 屏 HTML 可体验样张，并把要用户拍板的分歧点收成 ≤5 条决策表。

---

## 0. 读法与真相源关系

**本文管什么**：Agent 右栏（会话流 + composer + 结果卡区）的**最终交互形态**——21 形态每个的设计定稿、右槽互斥（Agent↔拆解）的完整交互、composer 收簇（7→≤5）、C1-C9 全部闭环的设计答案。

**本文不管什么**：8 态词表/工作方式×批准的**语义定义**（那是 `nomi-agent-interaction.md` #194 §2/§14 的真相源，本文只消费不覆盖）；后端 agentLoop / 工具 schema / 画布节点视觉（主设计系统 §4/§5）；M 线里程碑排期（`agent-m-line-rulings.md`）。

**与既有文档的关系**：
- `nomi-agent-interaction.md`（#194）= 交互**语义合同**（21 形态清单、8 态、双轴词表、控件规格、动效 token）——本文是它的**视觉/布局落地**，形态编号与它逐字对齐。
- `2026-08-31-agent-interaction-synthesis.draft.md` = **五源汇编 + 18 条样张指导 + 12 条不一致**——本文把那 18 条指导逐条兑现到具体屏。
- `RESIDENT-SHELL-DECISION.md` = 过渡版真机对账（21 形态 ✅10/🟡5/❌6 + C1-C9 条件）——本文是对那 9 条条件的**设计答案**。
- `2026-09-01-right-slot-agent-workspace-opinion.md`（③合流拍板）+ `2026-09-01-video-deconstruction-v1.md` §4.1（R-C-1~7）= 右槽终局定义——本文把它落成互斥切换的**具体动效与角标规格**。

**样张的底**：以 `/Users/aoqimin/Desktop/Nomi-shell-mockup/.mockup-shots/` 的 11 张过渡版真机截图**实际布局**为基版（右栏 340px 停靠画布右缘 + header 三行 + 会话流 + composer 底栏 7 控件），只画**改动**，不脑补新排版。拆解互斥态以 `docs/design/mockups/2026-09-01-video-deconstruction-v1/` 视图 06/07 为底。

---

## 1. 最终版三条结构决定（先定骨架，21 形态挂其上）

过渡版已经把地基做对了（8 态词表逐字落契约、状态色 4 组、role=log+回到最新、intentRole、引用后已变化）。最终版**不推翻地基**，只在三处结构上升级——这三处正是「过渡载具 → 最终形态」的分野：

### 决定 A：右栏从「单区会话流」升级成「双区 Agent 工作区」（③合流落地）

**过渡版**：右栏只有一条会话流（header → 上下文架 → 会话 transcript → 队列 → composer），拆解结果面板与它**互斥抢槽**。
**最终版**：右栏 = **Agent 工作区**，纵向双区：

```
┌─ Agent 工作区（右槽 · assistantWidth 340px）────────┐
│ ▛ header：品牌 · 线程 · 上下文用量 · 常驻技能 · 费用   │  ← 常驻顶条（形态 1/21）
│ ▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔ │
│ ▛ 结果卡区（固定 · 不随聊天滚走）─────────────┐    │  ← 新增：③合流的「固定结果卡区」
│ │ [拆解结果 · 5 镜 ▾]  [其它可操作工件…]      │    │     collapse≠unmount，勾选/滚动/Run 状态保持
│ ▙▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁┘    │
│ ▛ 会话流（role=log · 底部锚定 · 回到最新）────┐    │  ← 主体：指挥/追问/进度/回执
│ │  用户气泡 / 思考条 / 工具条 / 计划卡 …       │    │
│ │                              [↓ 回到最新]   │    │
│ ▙▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁┘    │
│ ▛ 指令队列（忙时 · 输入框上方）─────────────┐    │  ← 形态 18，折叠显数量
│ ▙▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁┘    │
│ ▛ composer：上下文架 · 输入 · 底栏配置行 ────┐    │  ← 收簇后 ≤5 簇
│ ▙▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁┘    │
└──────────────────────────────────────────┘
```

**为什么这样改**：用户 2026-09-01 拍板③合流（`right-slot-agent-workspace-opinion.md`）——「新功能 = 新卡片，不再新立面板」。过渡版的「互斥抢槽」是**权宜**（epic 未落地时的让位策略，`video-deconstruction-v1.md` R-C-1 明标「过渡行为，落地后自然消失」）。最终版把互斥消掉：拆解卡进结果卡区、会话流在下方，**同壳并存**。这是 C9 的结构答案（见 §5）。

**结果卡区 vs 会话流的分工**（`right-slot-opinion.md` 七维论证 2/3）：需要**审阅/操作**的高密度工件（拆解分镜表、未来可操作产出）停结果卡区、**不随聊天滚走**（「对照修改」是最高频后续动作）；过程性的（追问/进度/回执/思考）在会话流、可滚走。判据 = §3 视觉层级的延伸：**「产物本身且需要持续操作」→ 结果卡区；「过程性/一次性」→ 会话流**。

### 决定 B：composer 底栏 7 控件收簇成 ≤5（过 §1.5）

**过渡版**：底栏 7 个常驻控件 `📎 @ 🔧 ✏️ ⚙️ 🔒 🤖 ↑`（附件/引用/技能/提示词/工作方式/审批/模型 + 发送），已顶满 §1.5 的 L1 ≤5 功能簇预算、零 headroom。
**最终版**：按 §1.5.3「先分组→再去重→再归位→最后才收纳」收成 **5 簇**（详见 §4，形态 19/composer）。核心手法是**归位**（不是收纳）：上下文架/技能标记本就是 §13.2 说的「L1 输入反馈，不另占工具栏入口」——把 `@`(引用) 和技能触发从底栏搬到**上下文架自己那一行**，底栏只留「输入配置」这一类心智。

### 决定 C：工作方式轴掰回 §14 的 3 档，补回「编辑选中」（C1）

**过渡版**：工作方式私造 4 档 `ask/guided/balanced/auto`（自主强度），且**丢了 `编辑选中`**，并把审批语义折进工作方式文案——这是唯一的**契约级方向偏离**（`RESIDENT-SHELL-DECISION.md` C 段）。
**最终版**：工作方式 = §14.1 的 3 档 `Ask / 编辑选中 / Agent`（agent 能碰什么），批准策略 = §14.2 的 3 档（问多少），**两根正交轴不串**。这是 C1 的设计答案（见 §5 + 决策表 D1）。

---

## 2. 状态词表与颜色（承 #194 §2 · 本文不改，只登记样张取色）

样张所有状态色**只从这 5 组取**，出现第 6 种 = 错（来源 #194 §2.3）：

| 组 | 含哪些态 | 亮色 token | 暗色 token |
|---|---|---|---|
| 中性 · 在走 | `drafting` `queued` `running` | `--nomi-ink-40` = `oklch(0.68 0.01 80)` | `oklch(0.62 0.012 85)` |
| **要你动手** | `proposed` · 反问 · 付费 | `--nomi-accent` = `oklch(0.55 0.13 250)` | `oklch(0.7 0.12 250)` |
| 成了 | `done` · 写入回执 | `--workbench-success-ink` = `#248a3d` | `#7ee8aa` |
| 出事了 | `failed` `stopped` | `--workbench-danger` = `#ff3b30` | `#ff6961` |
| 有偏差 | `done + deviated` | `--nomi-warning` = `oklch(0.62 0.14 75)` | `oklch(0.78 0.13 75)` |

8 态 `drafting/proposed/declined/queued/running/done/failed/stopped` + 2 标志 `retryable`(挂 failed)/`deviated`(挂 done) —— 过渡版已逐字落 `projectAgentContracts.ts`，最终版继承不动。

---

## 3. 三视觉层级 + 结果卡区（承 #194 §3 · 扩一层）

| 层级 | 什么进来 | 形态 | 住哪 |
|---|---|---|---|
| **一行** | 过程性、不需你做事 | 思考条/阶段线/工具条/排队/进度/写入回执/压缩线/上下文用量 | 会话流 |
| **可展开的一行** | 过程性但你可能想看细节 | 思考条(展开看推理)/计划行(展开改单镜) | 会话流 |
| **卡片** | ① 要你做决定 ② 产物本身 | 计划卡/付费卡/反问卡/失败卡/有出入卡/产物卡/多候选组 | 会话流（一次性决定）**或** 结果卡区（需持续操作） |
| **结果卡区**（新）| 产物本身 **且** 需持续审阅/操作、不该被滚走 | 拆解分镜表 + 未来可操作产出 | 结果卡区（固定） |

**判据**：付费卡/反问卡/失败卡/有出入卡都是「决定一次就完」→ 留会话流随流；拆解分镜表是「对着它逐镜改，反复用几十分钟」→ 进结果卡区固定。多候选组是边界情形：暂存期在会话流（挑版），`accept` 后落画布——不进结果卡区。

---

## 4. 逐形态设计定稿（21 形态 · 每个：过渡版现状 → 最终版设计 → 改动理由+来源）

> 图例：现状判定沿用 `RESIDENT-SHELL-DECISION.md` 对账（✅ 已实现/🟡 部分/❌ 缺失）。「样张」列指向 §6 哪一屏能看到。

### 4.0 v2 映射表（21 形态 × 源 × v2 改了什么 · 用户四条裁决的落账）

> 源分布：**Beautiful UI 9 件 · AI Elements 6 件 · 竞品还原 2 件 · 自家整件复用 3 件 · 自造 1 件**（另：composer 整体解剖=Beautiful UI Prompt Bar、拆解表=竞品还原，见表尾）。库解剖抓自现役页面（2026-09-02 WebFetch），竞品证据 file:line 全在仓内。

| # | 形态 | 源（复用到的解剖细节） | v2 改了什么 |
|---|---|---|---|
| 1 | 上下文用量 | **AI Elements · Context**：trigger=百分比 pill，popover=环形进度+input/output 分项+费用 footer | header 三段文字（本轮/累计/费用）收成一颗「还能聊 ~N 轮」pill，明细与费用悬停才出——header 常驻信息 7 段→4 段 |
| 2 | 压缩分隔线 | **AI Elements · Checkpoint**：icon+触发钮+分隔线，内联于会话流 | 结构不变；「展开」= Checkpoint 触发钮位 |
| 3 | 用户气泡 | **AI Elements · Message** | 不动 |
| 4 | 思考条 | **Beautiful UI · Thinking**（可展开推理迹）+ **Loading State** 的 elapsed 计时「0.0s」 | 落定句保持；进行态计时「已等 0:04」对齐 BUI elapsed counter |
| 5 | 阶段分隔线 | **自造**（工序图示=受管动效资产，Playbook 5 阶段词表 Nomi 独有；线骨架承形态 2；LibTV.md:80-81 P83-84「拆解→资产→分镜→批量生图→批量生视频」印证流程分段心智） | 不动。自造理由：两库无「生产阶段」概念，竞品只有流程文字无阶段线控件 |
| 6 | 工具条 | **Beautiful UI · Tool Chips**：折叠 chip=计数+摘要（"4 tool calls, 2 messages"） | 折叠头改计数摘要语法「工具 3 · 1 在跑 · 0:12」 |
| 7 | 普通回复 | **Beautiful UI · Streaming Text** | 扫光光标保持；BUI 的 follow-up suggestions **不引入**——裁决③（每屏可见选择最少）压裁决①（整件复用），suggestion 行是+2 个常驻选择 |
| 8 | 计划卡 | **自家 `AgentPlanCard`（396 行）整件复用** · AI Elements Plan 交叉核结构 | 不动 |
| 9 | 付费确认卡 | **竞品还原·反着做**：LibTV_CLI.md:115（P98 连「失败是否扣积分」都不承诺）→我们单价+合计+冻结项全明示；明细复用 `SpendConfirmDialog` | 「不再提示」说明缩短一半 |
| 10 | 写入回执 | **自家 `CommittedProposalCard`**（lost-edits 中间态）；LibTV_CLI.md:186-187 印证「结果写回画布节点」 | 不动 |
| 11 | 排队 | **Beautiful UI · Task Rows**：leading 状态徽标 + title + trailing 计量 | 行解剖对齐（与 12 同源同行） |
| 12 | 进度条目 | **Beautiful UI · Task Rows** running 态（数字进度） | 同一行三态原位更新保持；无百分比不画条不变 |
| 13 | 产物缩略卡 | **AI Elements · Artifact**：header=标题+描述+动作组，content=纯内容 | 动作行从卡底挪进 header 右侧（改/重跑=icon+tooltip、去画布=accent 文字钮）；缩略图永不被盖（§8.3 顺带更稳） |
| 14 | 多候选组 | **竞品还原**：InvokeAI staging 合同（accept 才转正）+ XYQ `xyq_canvas_manual.full.dom.md:258`（版本管理只给「查看已存版本」单入口） | 砍「上一个/下一个」（点缩略图即选）：可见选择 6→4 |
| 15 | 失败条 | **Beautiful UI · Task Rows** failed 态 + 是否计费明示（竞品反证同 9） | 不动 |
| 16 | 有出入卡 | **自家 `ReconcileDeviationCard`（233 行）整件复用**；竞品 19 项能力对账矩阵均无此形态（`2026-08-24-agent-workbench-comparison.md` §3）＝护城河 | 不动 |
| 17 | 反问卡 | **Beautiful UI · Approval Card**：问题在顶、选项竖排成钮、分页「1 / 3」、底部 Skip+Continue；TapNow `tapnow_brainstorm.full.dom.md:190`「Agent 提出**一个**关键问题或少量候选」同心智 | **整件重构**：2 问同屏→一次一问分页 1/2；chips→竖排选项钮；加「跳过」；同屏要想的问题 2→1、可见选择 6→5 |
| 18 | 指令队列 | **AI Elements · Queue**：section trigger=icon+计数+label；item=indicator+content+描述+**hover-revealed actions**+attachment | 行动作 hover 才显（常驻可见控件 5→0）；加待办 indicator 圆点 |
| 19 | 技能 chip | **Beautiful UI · Prompt Bar** top chips zone（@ sources 归架） | 归位保持；架上两个虚线加号并成一个「+ 引用/技能」（2→1，@ 键入仍直达） |
| 20 | 技能载入条 | **Beautiful UI · Tool Chips**（工具条一员，专属文案） | 不动 |
| 21 | 常驻技能标记 | **AI Elements · Context** 的 trigger/popover 语法移植 | 裸文字改 pill+悬停说明（约束范围/作用阶段/版本/怎么停用） |

**表尾（非 21 编号件）**：composer 整体 = **Beautiful UI · Prompt Bar** 双区解剖（top chips zone + bottom controls：@ sources / commands / model picker——对位我们的上下文架 + 5 簇配置行）；拆解表 = **竞品还原**：LibTV.md:86（P90 生成范围自由选「单个/部分/全部」→勾选列）+ LibTV.md:107（P116「先检查 shot 信息再批量」→表格先审后跑）+ TapNow `tapnow_brainstorm.full.dom.md:145`（「回到画布查看已确认内容，不需要在很长的聊天记录里重新寻找」→结果卡区固定不随聊天滚走）。

### 形态 1 · 上下文用量（一行 · header）
- **过渡版**：✅ header 显「本轮 0 · 累计 0 tokens」（`ProjectAgentResidentShell.tsx:698` 读 `agentUsageStore`，#194 点名「白记无人读」已补）。
- **最终版**：保留在 header 第一行，与常驻技能标记（形态 21）**同排**：`Nomi · 未命名线程 │ 本轮 1.2k · 累计 8.4k · 还能聊 ~40 轮 │ 按 编剧·Kasdan │ 费用 ¥0.00`。数字用 `tabular-nums`，接近上限时「还能聊」变 warning 色。
- **改动理由**：#194 §4 形态 1 要求「用了多少 / 还能聊多久」——过渡版只有「用了多少」，最终版补「还能聊 ~N 轮」的余量投影（承 S8）。样张：屏 A header。
- **v2**：照 AI Elements Context 解剖收成**一颗 pill**（环形进度 + 「还能聊 ~40 轮」），本轮/累计/费用全部进悬停 popover（footer=费用，同 Context 的 cost footer）——header 常驻信息 7 段→4 段，负荷最低裁决③。
- **v3**：header 收成**一行到底**：`N Nomi · 用量 pill · 历史 icon · 收起 icon`。线程名（「· 雨夜追逐」）删（整改⑧，顶栏已有项目名）、「当前现场」行删（整改⑧）、常驻技能 pill 删（整改①②，形态 21 并入对话流事件行）。popover 词汇过闸：「上下文 · 已用 14%」改「这轮对话还能聊多久」，「tokens」改「字符量」。

### 形态 2 · 压缩分隔线（一行）❌→✅
- **过渡版**：❌ 全 resident 无 compaction 渲染（自动压缩时用户会觉得 AI 突然失忆，`RESIDENT-SHELL-DECISION.md` 形态 2）。
- **最终版**：会话流中一条**居中细线 + 文字链**：`—— 前面 24 轮已折叠 · 展开 ——`。点「展开」就地展开被折叠的轮次（不跳走、随会话滚）。线用 `--lineS`，文字 `--nomi-ink-40`，无高度（不是控件，是分隔物）。
- **改动理由**：#194 §4 形态 2 + S6「不给这条，自动压缩发生时用户只会觉得 AI 突然失忆」。参考现役 `staleConversationDivider.tsx` 的近邻视觉（synthesis 节 3I）。样张：屏 A 会话流顶。

### 形态 3 · 用户气泡（一行）
- **过渡版**：✅ `kind==='user'` → 右对齐 ink 气泡（`:709`）。
- **最终版**：不动。右对齐、`--nomi-ink` 底、paper 字、圆角 10px（复刻屏 07 真机气泡）。
- **改动理由**：过渡版已对，无需改。样张：屏 A。

### 形态 4 · 思考条（可展开的一行 · 结束落定不消失）🟡→✅
- **过渡版**：🟡 planning 时显示可展开条，但**结束时未落定成结果句**，planning 结束即消失（`RESIDENT-SHELL-DECISION.md` 形态 4，正是 AI Elements 也没保证那条）。
- **最终版**：进行时 = 工序图示（形态 5 的受管动效资产）+ 片场话 + 计时「正在画分镜 · 已等 0:04」，可点展开看推理步骤；**结束时原位落定成结果句不消失**：`想了 4 秒 · 拆成 5 镜，2 镜要站位参考 ▸`（灰身、可展开回看）。用 `--nomi-motion-settle` 420ms 从「进行」淡定成「结果」。
- **改动理由**：#194 §4 形态 4 + §5「结束时落定成结果，不能就地消失」+ C6。这是 Nomi 对 AI Elements Chain of Thought 的差异点（synthesis 节 4：AI Elements 文档没说完成后落定还是消失）。样张：屏 A。

### 形态 5 · 阶段分隔线（一行）❌→✅
- **过渡版**：❌ 无「进入·生产段」阶段线；工序图示阶段词表未做成受管动效资产（`RESIDENT-SHELL-DECISION.md` 形态 5）。
- **最终版**：会话流中一条**带工序图示的居中线**：`◆ 进入 · 生产段`（图示 = 该阶段的受管动效资产，见下）。阶段词表沿用 Playbook 5 阶段 `script|storyboard|build|generate|assemble`（单一真相源 `skills/brand-promo/skill.json`，**不另造第四套**）。
- **工序图示（受管动效资产 · 只能由 `src/design/` 提供）**：读剧本=一页纸3行(1.9s)／拆镜头=3横格依次落位(2.1s)／搭画布=取景框四角对焦呼吸(2.4s)／出片=场记板打板(1.9s)／上时间轴=3轨道左走(2.0s)。**不用品牌 N**（语义撞车，#194 §5）。
- **改动理由**：#194 §4 形态 5 + §5 + S7 + C5。样张：屏 A（阶段线 + 工序图示动画）。

### 形态 6 · 工具条（一行）
- **过渡版**：✅ `ResidentToolChips`（action icon + effect + target + result + elapsed，可折叠分组，`:708`）。
- **最终版**：不动核心。完成时描边勾（`--nomi-motion-settle`）：`读了剧本 · 2400 字 ✓`；多条完成折叠成一行摘要 `工具调用 3 ▸`（屏 07 已示范 progressive disclosure）。
- **改动理由**：过渡版已对。样张：屏 A（含折叠态）。
- **v3（整改⑤ · 改形）**：v2 的边框底色面板区（toolgroup 盒）删除。**进行中 = 当前工具一行**（转圈 icon + 人话 + 已用时长 + 行尾 ▾，点开看本轮已完成步骤）；**结束 = 整段收拢一行总览**（`6 步 · 读剧本 · 拆 5 镜 · 生成 2 图 ▾`，展开逐步明细）。Claude Code 同款：每个工具一行、可展开、完成后收拢。同一行原地变 3 态（排队→进行→完成，承形态 11/12），无百分比不画进度条不变。

### 形态 7 · 普通回复（一行）
- **过渡版**：✅ `ResidentStreamingText`（流式扫光光标，`:711`）。
- **最终版**：不动。纯文本、流式期扫光光标（`--nomi-motion-sweep`）。
- **改动理由**：过渡版已对。样张：屏 A。

### 形态 8 · 计划卡（卡片 · 批级+单镜两层）🟡→✅
- **过渡版**：🟡 resident 不引 `AgentPlanCard`；生成提案走 `GenerationProposalEditor`（prompt-on-top + `InlineParameterBar`，§9.3 满足），但「逐镜勾选+单镜覆盖」的多镜 plan-card 未呈现（`RESIDENT-SHELL-DECISION.md` 形态 8）。
- **最终版**：接进 `AgentPlanCard`（现役 396 行已实现批级+单镜两层，synthesis 节 3B）。卡顶「这批用」= 完整 `InlineParameterBar`（模型/比例/清晰度由**模型档案声明**，不手写）；每行默认「同上」，改过显「已改 10s · 还原」，点行展开该行参数条。AI 配的没动过的参数用蓝底 accent「待你看」chip。**绝不画有 ▾ 但无交互的假下拉**（`AgentPlanCard.tsx:59` 诚实纪律）。一次「确认全部」原子批准。
- **改动理由**：#194 §9 + S15/S16 + 铁律「不在对话卡里另造参数 UI」。复用现役 `AgentPlanCard`（一致性>新造）。样张：屏 B（计划卡）。

### 形态 9 · 付费确认卡（卡片 · 对话内富卡）❌→✅
- **过渡版**：❌(resident) `SpendConfirmDialog` 仍是唯一宿主；#194 §10「对话流内付费富卡」第二宿主未在 resident 落地（`RESIDENT-SHELL-DECISION.md` 形态 9）。
- **最终版**：会话内**付费富卡**（agent 对话时的第二宿主，与 `SpendConfirmDialog` 共用同一 `SpendConfirmRequest` + 同一份明细组件，一语义两投影）。卡上必须有：**每项单价 + 合计**（`3 镜 × ¥0.42 = ¥1.26`）+ **冻结项明说**（「过闸后不能再改：镜头数、模型、时长、清晰度。提示词仍可改」）+「本会话不再提示」（用户直发档）/ 倒计时（外部 MCP 档）+ **交互即暂停**（用户点/移入/聚焦 → 倒计时停、文案切「已暂停·你正在查看」，复刻 `SpendConfirmDialog.tsx:37-69`）。对话暂停等回答。
- **改动理由**：#194 §10 + S12 + C3 + 「没有一家卡上显示价格，反着做就是护城河」（§1③）。样张：屏 B（付费富卡）+ 决策表提示两宿主明细一致。
- **v3（整改⑥⑨ + D4 落卡）**：agent 受理档卡上**无「本会话不再提示」开关、无倒计时**（倒计时/交互即暂停只属外部 MCP 档；「不再提示」只属用户直发档——D4 建议直接画进样张）。词汇过闸：「过闸后不能再改」→「付费后就不能再改」；确认钮带金额「确认并生成 ¥1.26」。卡内可见选择 3→2。

### 形态 10 · 写入回执（一行 · 护城河）
- **过渡版**：✅ 已批准回执 + 去画布定位钮（`:712` `data-agent-proposal-receipt`）。
- **最终版**：`已加 5 个节点 · 撤销`（成了色）。**「整笔撤销」双确认**：若检测到用户改过提议节点，先列明将丢失的修改再等第二次确认（复刻 `CommittedProposalCard.tsx` 的 lost-edits 中间态）+ 落点回报 chip（本笔落进哪些分类，非当前分类给跳转 chip）。
- **改动理由**：#194 形态 10 + §11 护城河「提议事务+整批撤销」+ synthesis 节 3C 「样张的撤销要体现 lost-edits 中间态」。样张：屏 A（回执 + 撤销双确认态）。

### 形态 11 · 排队（一行）
- **过渡版**：✅ `ResidentTaskRows`（排队/进行中/待确认计数+位次，`:726`）。
- **最终版**：`排队中 · 前面还有 3 个`，位次前移时数字滚动（`--nomi-motion-settle`，`n.tk` 手法）。与形态 12 是**同一条 id 在变**（原位更新不刷屏）。
- **改动理由**：#194 形态 11 + §1① 原位更新。样张：屏 A。

### 形态 12 · 进度条目（一行 · 原位更新）
- **过渡版**：✅ 工具项 running 态原位扫光，无百分比不画进度条（诚实纪律继承，`ResidentUiPrimitives.tsx:59,118`）。
- **最终版**：`排队中 · 前面还有 3 个` →（同一行变）`进行中 · 生成 3/5` →（同一行变）`完成 · 5 镜`。**没百分比不画进度条**（画空槽会被读成分隔线+假装知道进度，`TaskCenterPanel.tsx:388` 诚实纪律）；云端提交后停不下来 → 显 `🔒 已提交·钱已花`不给假取消。**同屏最多 1 处扫光**（多任务并行只当前活跃那条动、其余静态百分比，#194 §7.3）。
- **改动理由**：#194 形态 12 + §1① + §7.3 + S8。样张：屏 A（同一行三态 + 单点扫光）。

### 形态 13 · 产物缩略卡（卡片）🟡→✅
- **过渡版**：🟡 artifact/task 行 + 「去画布/打开」钮，是**行**不是带缩略图的**卡**（`RESIDENT-SHELL-DECISION.md` 形态 13）。
- **最终版**：升级成**带缩略图的卡**：缩略图（图片 tile / 视频 poster+时长 / 音频波形+时长 / 文档类型+页数/字数 / 3D 相机截图+视角名）+ 尺寸/版本副行 + 缩略图**外部就近动作行**「改 · 重跑 · 去画布」（**动作不覆盖内容**，禁半透明按钮盖图，#194 §8.3）。「去画布」实际执行：定位+缩放到可读+短暂高亮，保留对话滚动位置。
- **改动理由**：#194 形态 13 + §17.1 多媒体回显矩阵 + §8.3「动作不许压在内容上」。样张：屏 B（图片产物卡 + 外部动作行）。
- **v2**：照 AI Elements Artifact 解剖重构——**header 一行 = 标题 + 尺寸/版本描述 + 右侧动作组**（改/重跑 icon+tooltip、去画布 accent 文字钮），缩略图是纯 content。动作离开卡底、更近标题，图面永不被盖。

### 形态 14 · 多候选组（卡片 · select/accept/discard）🟡→✅
- **过渡版**：🟡 `ResidentBatchStack` 做了 §9.4 批量**输入**镜横向三页堆叠；同一请求多**结果**候选暂存(accept 才转正)未见（`RESIDENT-SHELL-DECISION.md` 形态 14）。
- **最终版**：同一生成请求多结果先进**暂存组**：并排最多 3 版（多的折叠），每版 `select/previous/next`，组底「采用这版」= `accept`（accept 后才成正式画布版本，复刻 InvokeAI staging 合同）。**批量卡（翻多个输入镜头）≠多候选卡（翻同一请求多结果）**——视觉区分：批量卡右上角「3/8」翻页、多候选卡是并排缩略图（状态源/确认语义/去画布目标分开，#194 §9.4）。
- **改动理由**：#194 形态 14 + §17.2 候选暂存 + S9。样张：屏 B（多候选组 3 版并排 + 采用）。
- **v2**：砍掉「上一个/下一个」两钮——3 版并排时点缩略图即选，翻页钮与之重复（可见选择 6→4，裁决③）；XYQ 同场景也只给「查看已存版本」单入口（`xyq_canvas_manual.full.dom.md:258`）。`previous/next` 语义保留给 >3 版折叠时的键盘/折叠导航，不做常驻钮。

### 形态 15 · 失败条（卡片）
- **过渡版**：✅ 人话原因 + 是否计费隐含 + 3 等宽下一步钮（`:713`，屏 07 已示范「换模型重试/改提示词/看日志」）。
- **最终版**：保留。哪步败 + **人话原因** + **是否计费明示**（不只隐含：「本次未计费」/「已扣 ¥0.42」）+ 下一步（按门类派生，红边 + `--workbench-danger`）。3 钮等宽均分、主次只用颜色。
- **改动理由**：#194 形态 15 + LibTV「错误修正闭环」（synthesis 节 2A）+ synthesis 节 2B「失败归因+是否计费明示=把无补贴劣势变信任优势」。样张：屏 A/B（失败卡，明标是否计费）。

### 形态 16 · 有出入卡（卡片 · 批准≠执行 · Nomi 独有护城河）❌→✅
- **过渡版**：❌(resident) 不引 `ReconcileDeviationCard`（它在 generationCanvas 存活）；#194 §11 明写「Nomi 独有，竞品都没有」的护城河形态未接进新壳（`RESIDENT-SHELL-DECISION.md` 形态 16 + C2）。
- **最终版**：接进 `ReconcileDeviationCard`（现役 233 行）。**正常对账一致时永不出现**（诚实纪律兜底面，非常驻）。每条偏差：`where`（源标题→目标标题）+ `fieldLabel` + 人话 `detailLine`（「哪些没按计划生效、为什么」，不甩原始 id）。动作按门类派生：结构边丢失=重连边／画面偏差=改 prompt 重生走确认闸（预算耗尽则隐藏）／`showUndo` 只对结构偏差／`exhausted` 时改显「已尽力」绝不无限回灌。**偏差词表 derive 自 `SHOT_VERIFY_DIMENSIONS`，样张文案也 derive 不硬写**。
- **改动理由**：#194 形态 16 + §11 护城河 + S10 + C2 + synthesis 节 3D。这是 AI Elements 与所有竞品都没有的 Nomi 独有面，新壳不能落在门外。样张：屏 B（有出入卡）。

### 形态 17 · 反问卡（卡片 · ≤3 问 chips）❌→✅
- **过渡版**：❌ 无 clarification/反问卡渲染分支（`RESIDENT-SHELL-DECISION.md` 形态 17 + C7）。
- **最终版**：会话内卡片，每问一行 + 选项 chips（药丸、同组等高等圆角）。**≤3 问合并**（TapNow 是 4，我们更克制）。chip 是**内容**（按内容定宽），选完点「继续」。要你动手 = accent 色。
- **改动理由**：#194 形态 17 + S11 + C7 + TapNow「合并提问≤4，我们≤3」（synthesis 节 2A）。样张：屏 B（反问卡分页）。
- **v2**：照 Beautiful UI Approval Card **整件重构**——一次只显一问（问题在顶）、选项**竖排成钮**（带序号，可键盘 1/2/3）、分页「1 / 2」、底部「跳过 + 继续」。同屏要想的问题 2→1、可见选择 6→5；TapNow 同心智「Agent 提出**一个**关键问题或少量候选」（`tapnow_brainstorm.full.dom.md:190`）。仍 ≤3 问合并、答完一次性继续。

### 形态 18 · 指令队列（一行 · 输入框上方 · 完整 TurnDraft）
- **过渡版**：✅ `ResidentTaskRows`（编辑/删除/上移/下移/暂停，折叠显数量+是否暂停，`:726`；屏 09 已示范）。
- **最终版**：保留。忙时输入框**不禁用**、回车入队（P0，不静默丢弃）；队列项 = 完整 `TurnDraft`（text + attachments + context 快照 + workMode + approvalPolicy + agentModelId），行内编辑/删除/上移/下移/暂停，附件/引用/模式/权限用图标+数量回显；折叠显「队列 · 排队 1」+ 是否暂停。区分 steer（纠正当前）vs nextTurn（下一条）。
- **改动理由**：#194 形态 18 + §4.1/§16 P0 + S20 + `main-session` 无关。过渡版已对。样张：屏 A（队列展开）。
- **v2**：照 AI Elements Queue 解剖——行 = indicator 圆点 + 内容 + 图标计量，**编辑/上移/暂停/删除 hover 才显**（Queue 的「hover-revealed actions」）：常驻可见控件 5→0，行扫一眼只剩「排的是什么」。
- **v3（整改④ · 改形）**：**排队 = 消息排队**。v2 的队列区块（区头「队列 · 排队 1」+ 停止钮 + 行内计量/管理钮）删除；排队消息 = **浅色行贴输入框上沿**：小圆点 + 消息文字 + 右侧小 ×（撤回），无卡片无边框无区头；轮到它自动进对话执行。Claude Code queued 消息同款。忙时输入框不禁用、回车即排（P0 不静默丢弃）不变；改单条内容 = 撤回重发（× 一下重打），不再做行内编辑/排序钮。

### 形态 19 · 技能 chip（composer 上方 · 持续可见）
- **过渡版**：✅ accent 药丸 + 可摘（`:727` `data-agent-reference=skill:*`）。
- **最终版**：挪到**上下文架那一行**（不占底栏入口）：`按 编剧·Kasdan 方法论 ✕`，与画布选中 chip/`@`引用 chip 同排。这是 composer 收簇（决定 B）的一部分。
- **改动理由**：#194 形态 19「必须持续可见——不然用户不知道输出为什么变了」+ §13.2「上下文架是 L1 输入反馈，不另占工具栏入口」。样张：屏 A（上下文架含技能 chip）。
- **v3（整改①②③ · 形态取消）**：上下文架整个删除——引用/定妆/技能全走**输入框内 `@`**（弹选择器，选完 = 句中 token 小色块，可删；「引用后已变化」的诚实态转为 token 变黄 + 悬停说明）。技能 chip 不再常驻任何位置；「持续可见」由用户 2026-09-02 总纲推翻——想知道当前约束，翻对话流的载入事件行（形态 20）。

### 形态 20 · 技能载入条（一行 · load_skill）🟡→✅
- **过渡版**：🟡 ToolActionIcon 认 `load_skill` 图标，但无「载入了 X」专属文案分支，靠通用工具条兜（`RESIDENT-SHELL-DECISION.md` 形态 20）。
- **最终版**：专属工具条文案：`载入了 镜头语言与摄影技法 ▸`（可展开看它是什么），属工具条那一层（形态 6 的一员）。载入后其约束反映到 header 的常驻技能标记（形态 21）。
- **改动理由**：#194 形态 20 + S8。样张：屏 A（技能载入条）。
- **v3（整改② · 唯一幸存者）**：技能在 UI 的**唯一**呈现 = 这一行：`⚙ 载入 编剧·Kasdan`（灰字小号、非卡片、悬停说明「Nomi 临时装上的一套写法」），出现在载入那一刻、随流滚走不驻留。形态 19 的 chip 与形态 21 的 header 标记都并进这一行（Claude Code 同款：载入是对话中的一行系统事件）。

### 形态 21 · 常驻技能标记（header · 持续）❌→✅
- **过渡版**：❌ header 只有上下文用量，无「常驻技能」标记（技能约束后续所有输出，不能只在 chip 闪一下，`RESIDENT-SHELL-DECISION.md` 形态 21 + C7）。
- **最终版**：header 第一行与上下文用量同排：`按 编剧·Kasdan`（accent，点开看作用阶段/版本）。技能约束的是**后面所有输出**，所以常驻 header、不只在载入那一刻闪。
- **改动理由**：#194 形态 21 + S8 + C7。样张：屏 A（header 常驻技能标记）。
- **v3（整改①② · 形态取消）**：常驻技能标记**删除**——用户 2026-09-02 总纲（一切事件进对话流、常驻栏越薄越好）推翻 #194 的「常驻 header」要求，与 v2 的双重载入区（顶部 pill + 架上 chip）一并清掉。技能状态只活在形态 20 的事件行；实现前同步改 `nomi-agent-interaction.md` §4 形态 19/21（C7 的「常驻标记」答案随之作废）。

> ★ 三个待补形态（音频播放器/音色选择/转写分段）随配音·TTS 进 Pack 剪辑段补，**现在只占位不实现**（#194 §4 脚注）；AI Elements Voice 类是现成结构参照（synthesis 节 4）。样张不画。

**最终版过账**：21 形态中 ✅ 全部有设计定稿。相对过渡版，本轮**补齐 6 个 ❌**（2 压缩线/5 阶段线/9 付费富卡/16 有出入卡/17 反问卡/21 常驻技能）+ **升级 5 个 🟡→✅**（4 思考落定/8 计划卡/13 产物缩略卡/14 多候选暂存/20 技能载入条）。

---

## 5. C1-C9 全部闭环的设计答案

`RESIDENT-SHELL-DECISION.md` 列了 C1-C8，`docs/qa/...red-lights.md` 侧另有 C9（开闸时旧 composer 态删除+拆解 handoff 重接）。逐条给设计答案：

| 条 | 类别 | 过渡版问题 | 最终版设计答案 | 归到 |
|---|---|---|---|---|
| **C1** | 方向·必须拍板 | 工作方式私造 4 档 + 丢 `编辑选中` + 审批语义折进工作方式 | 掰回 §14.1 的 3 档 `Ask/编辑选中/Agent`（agent 能碰什么）；「自主强度」并入批准策略轴；两轴正交不串。**这是 §14 用户 2026-08-27 亲裁的轴，不让 cutover 单方改** | 决策表 D1 |
| **C2** | 护城河 | 有出入卡（形态16）未接进 resident | 接 `ReconcileDeviationCard`，正常一致时不出现、偏差时按门类派生动作、词表 derive 自 `SHOT_VERIFY_DIMENSIONS` | 形态 16 · 屏 B |
| **C3** | 护城河·诚实 | 对话内付费富卡（形态9）第二宿主未落地 | 会话内付费富卡，与 `SpendConfirmDialog` 共用同一 `SpendConfirmRequest`+明细组件，明标单价+合计+冻结项+交互即暂停 | 形态 9 · 屏 B |
| **C4** | bug·R15 | 模型菜单空态把原始 i18n key `agentResident.modelMenuHint` 显给用户（屏 06） | 补键：菜单空态文案「去选一个文本模型 · 决定 Nomi 怎么理解和规划」。**v3.1**：按钮本体不再摆常驻文字——注意态 = 🤖 右上角小红点 + tooltip「去选文本模型」 | composer · 屏 A |
| **C5** | 形态·增量 | 缺压缩分隔线(2)+阶段分隔线(5) | 都做成「一行文字链/带工序图示的居中线」，就地展开、随会话滚 | 形态 2/5 · 屏 A |
| **C6** | 形态·落定纪律 | 思考条(4)结束即消失 | 结束原位落定成结果句不消失，`--nomi-motion-settle` 淡定 | 形态 4 · 屏 A |
| **C7** | 形态 | 缺反问卡(17)+常驻技能标记(21) | 反问卡 ≤3 问 chips；常驻技能标记进 header 与上下文用量同排 | 形态 17/21 · 屏 A/B |
| **C8** | 构建·已代修 | cutover 新增 11 图标漏扩 `tablerIcons.ts` 白名单，production build 红 | **纯设计包不涉代码**；但样张只用已在白名单的 Tabler 图标 + 文字（webfont CDN），不引入未登记图标；开闸实现时须先扩白名单再用（`RESIDENT-SHELL-DECISION.md` 已在过渡分支代修，实现班并轨即可） | 全样张（图标纪律） |
| **C9** | 开闸·QA | 旧 composer 态删除 + 拆解 handoff 重接（`red-lights.md` 侧） | **删旧（P1）**：开闸删过渡版 4 档工作方式契约、互斥抢槽逻辑（`generationAiCollapsed`↔`videoDeconstructionCollapsed` 互翻）——最终版右槽双区并存，互斥标志作废。**拆解 handoff 重接**：拆解卡从「互斥面板」平移进「结果卡区」，它订阅的是**工件状态**（`videoUrl/nodeId` 维度的 `DeconstructVideoResult`）不绑面板 open 布尔，所以平移不返工（`right-slot-opinion.md` 结果卡契约 + `video-deconstruction-v1.md` R-C-7）。两个入口（节点浮条 A / Agent 会话工具卡 B）汇聚同一张卡 | §1 决定 A + 屏 D + 决策表 D5 |

---

## 6. HTML 可体验样张（5 屏 · 真实布局为底）

样张文件：`docs/design/mockups/2026-09-01-agent-ui-final-redesign.html`（浏览器直接打开，顶部有标尺开关 + 亮/暗切换 + 屏切换 tab）。

以 11 张过渡版真机截图的**实际布局**为基版（右栏 340px 停靠画布右缘、header 三行、会话流、composer 底栏），只画改动。五屏：

| 屏 | 覆盖 | 关键改动点（相对过渡版）| v3 认知负荷账 |
|---|---|---|---|
| **屏 A · 主对话流** | header 一行（Nomi+用量 pill+历史+收起）· 会话流（压缩线2/阶段线5/技能载入事件行20/思考条落定4/普通回复7/工具一行+行尾折叠6/写入回执10+撤销icon/进行中行12）· 排队浅色行18 · composer（@ token 内嵌 + 底栏 5 簇全 icon）| v3：header 三行→一行（删线程名/当前现场/技能 pill，整改①⑧）；工具面板区→一行+折叠（⑤，样张真可点）；队列卡→浅色行（④）；引用架→输入框 @ token（③）；撤销 icon 化（⑦）。v3.1：底排全 icon + hover tooltip，模型未选 = 小红点 | 常驻可点 **19→10**（头 4→3 · 排队 5→1 · 输入区 10→6）· v3.1 底排常驻文字 **2 句→0** |
| **屏 B · 审批 + 付费卡 + 有出入卡** | 计划卡8 · 付费富卡9（agent 档：无「不再提示」无倒计时）· 有出入卡16 · 反问卡17 · 多候选组14 · 产物缩略卡13 · 失败卡15 | v3：全部文案过词汇闸（⑥：过闸→付费后、删校验分/阈值、v2→第 2 版、看日志→看详情）；卡内设计注释全部挪出卡外（⑨）；付费卡按 D4 删勾选框；计划卡参数去重+按钮改「生成已选（N 镜）」| 卡均可见选择 **4.3→4.0** · 卡内注释 **5→0** |
| **屏 C · 右槽互斥切换** | 左半：Agent 进驻 + 拆解收成源节点浮条；右半：拆解进驻 + Agent 收成顶栏角标「●进行中」；切换 260ms · 收起=collapse≠unmount | v3：mini 内工具事件改一行式、删内部话「(M 线后)」与「本地取证据/有对照」chips（⑥）| 不变（过渡态规格）|
| **屏 D · 结果卡固定 + 对话并存（最终态）** | 固定结果卡（卡头 📌，无区域标签；v3.1 默认收成细条「📌 拆解结果 · 5 镜 · 已选 2 ▾」，点开全表封顶≈视口 45% 表内滚，样张真可点）+ 下方会话流并存；两入口汇聚同一张卡 | v3：「结果卡区·固定·不随聊天滚走」标签删（⑧，pin icon 自说明）；header 同屏 A 一行；工具行同款一行式；拆解表 disclosure chips 删（⑥⑨）。v3.1：细条默认收起、摘要常在 | 常驻可点 **12→10→9**（v3.1 收起时结果卡整块=细条 1 个）|
| **屏 E · 布局（v3.2）** | A 四个面 + 浏览器的落位图（创作/分镜/生成/预览/浏览器接管）· B 缩小三档（收起角标条 · 窄窗断点表 · 最小窗 1100×720 实数图）· C 三能力标注（拖宽手柄/热区/光标 · 放大 · 拖走浮动 + 换边否决理由） | v3.2 新增；静态标注图（手柄/热区画位置和数字，不实现拖拽 JS）；数字全来自真实外壳 file:line，规格见 §11 | 纯布局说明页，不计入交互负荷账 |

样张硬约束（synthesis 节 6 逐条 + 本文）：状态色只 5 组；控件统一 28px 高·同排等宽均分·主次只用颜色·无自造 xs(24px)；不画假下拉 ▾；没百分比不画进度条；原位更新不刷屏·同屏 1 处扫光；上下文架不占工具栏入口；工作方式只 3 档；忙时队列不静默丢弃；付费卡两宿主明细一致；暗色单独验（过渡版「只验亮色」缺口本轮补）。

---

## 7. composer 收簇账（7→5 · 过 §1.5）

**过渡版底栏 7 控件**（顶满 L1 ≤5 预算、零 headroom）：📎附件 · @引用 · 🔧技能 · ✏️提示词 · ⚙️工作方式 · 🔒审批 · 🤖模型 · ↑发送。

**收簇手法（§1.5.3 优先级：先分组→再去重→再归位→最后才收纳）**：

| 原控件 | 手法 | 去哪 | 代价 |
|---|---|---|---|
| @引用 | **归位** | 上下文架那一行（§13.2「L1 输入反馈不另占入口」）——`@` 触发从底栏移到上下文架的「+ 加引用」 | 0 |
| 🔧技能 | **归位** | 同上，技能 chip 与引用 chip 同住上下文架，`+ 技能` 触发在那一行 | 0 |
| ⚙️工作方式 + 🔒审批 | **分组** | 合成一个「执行方式」簇：一个触发点开二级面板，上半工作方式(3档)、下半批准策略(3档)——**两轴仍正交、只是同一个入口的两段**（不是并成一个选择器，那会违反正交，synthesis 节 6.13） | +1 点击（但从 2 簇降到 1 簇）|

**收簇后底栏 5 簇**：📎附件 · ⚡执行方式(工作方式+审批二段) · 🤖Agent模型 · ✏️提示词选择器 · ↑发送。

**为什么这样收**：@/技能是「输入内容的补充」（属上下文架心智），工作方式/审批是「这一轮怎么执行」（属执行心智），模型/提示词是「用什么理解」——归位后底栏只剩「配置这一轮怎么发」一类心智。**不往 ▾ 硬塞**（§1.5.4 反例：为凑数收中频控件=净负收益）；@/技能是**归位**（0 代价）不是收纳。上下文架/技能标记/上下文用量本就不该占底栏（#194 §13.2），过渡版把它们的触发混进底栏才顶满预算。

**决策表提示**：附件也可再归并进「执行方式」或独立——见决策表 D3（附件是否保留独立触发）。

**v3 补账（整改③）**：「归位到上下文架」进一步收成「**归位到输入框本身**」——架整个删除，@/技能/定妆全部走输入框 `@`，引用即句中 token（0 常驻件）。底栏仍 5 簇不变；§13.2「不另占工具栏入口」达成方式从「架」变「@ token」，L1 常驻件比 v2 再少一排。

**v3.1 补账（用户裁决「太多字了冗余」）**：5 簇**全 icon 化**——📎⚡🤖✏️ 去文字标签只留 Tabler 图标，hover tooltip 给完整含义（「Agent · 安全自动」「文本模型 · Kimi K2」「提示词模板」），↑ 发送不变。注意态改红点：模型未选 = 🤖 右上角小红点 + tooltip「去选文本模型」，不再占常驻文字（C4 v3.1 形态）。簇数、可点数都不变（仍 5），变的是**常驻文字 2 句→0**——低频配置不配在最高频视线落点常驻占字。

---

## 8. 右槽互斥的完整交互（Agent↔拆解 · 动效 + 角标）

> 终局是③合流双区并存（§1 决定 A + 屏 D）；**过渡期**（交互 epic 双区外壳落地前）走互斥（`video-deconstruction-v1.md` R-C-1~2 标「过渡行为，落地后自然消失」）。本节规定过渡期互斥的动效/角标规格 + 终局并存的切换。

### 8.1 过渡期互斥（屏 C）
- **右槽同一时刻一个住户**：`{ Agent 栏 | 拆解面板 }` 二选一。
- **拆解开 → Agent 收成顶栏角标**：落 `NomiAppBar` 右簇，紧挨「配置」组左侧，复刻现役 ghost 钮（`h-[30px]` + `IconX size=15 stroke=1.8`）。**有新动静**（新回复/工具跑完）冒 accent 小圆点或数字（语法复刻 `TaskCenterButton.tsx:143-147`：`min-w-4 rounded-pill bg-nomi-paper text-micro tabular-nums text-nomi-accent`）。点角标=还原右栏。落顶栏因为它是唯一跨创作/生成/预览常驻的 chrome。
- **Agent 开 → 拆解收成源节点浮条 + 节点角标**：源视频节点头挂「已拆解·N镜」角标，节点内挂「拆解结果·N镜」浮条。点浮条=展开右槽那张表。落节点因为拆解结果本就属那条源视频（一功能一个家）。
- **动效**：进驻用 `--nomi-motion-enter` 260ms 滑入；让位者淡出成角标/浮条（260ms）。**收起=collapse≠unmount**：勾选进度/滚动位置/进行中 Run/对话上下文，收起后点回原样还在。两个收起标志在同一 store 事务里互翻（避免两个都展开的中间态）。
- **切换动效纪律**：滑入/淡出走 enter(260ms)；角标新动静脉冲走 settle(420ms) 单次；**不同屏两处扫光**（切换期只一处活跃）。

### 8.2 终局并存（屏 D · 互斥消失后）
- epic 双区外壳落地 → 互斥标志作废（C9 删旧）。拆解卡进**结果卡区**（固定、不随会话滚）、Agent 会话流在下方，**同壳并存**。
- **v3.1：折叠是默认态**——细条 `📌 拆解结果 · N 镜 · 已选 N ▾`（高度≈一条工具行，摘要随勾选实时变）；点开全表**封顶约视口 45%**、表格内部滚动，再点收回细条。收起=折叠不卸载：勾选/滚动位置全保持。屏 C 过渡期的节点浮条/顶栏角标语义不变。
- 两入口汇聚同一张卡：节点浮条(A) / Agent 会话内工具卡(B) 都写同一个 `DeconstructVideoResult` 槽（`videoUrl/nodeId` 维度），渲染进同一张卡——绝不各开一张（`video-deconstruction-v1.md` R-C-7）。

### 8.3 宽度单一真相源
- 结果卡区/Agent 栏宽度都用 `assistantWidth`（`workbenchStore.ts:233` 默认 **340**，clamp 300–600）→ `--generation-assistant-target-width`。**#251 的 520px 作废**，不自立第二个宽度数字。样张用 `--nomi-assistant-width: 340px`。

---

## 9. 实现期缺口（不许假装做完 · 承 #194 §12）

| # | 缺口 | 归属 |
|---|---|---|
| 1 | 本文仍是交互/视觉方案，生产代码尚未实现（开闸后 M 线交互层落地）| Track B5 / M 线 |
| 2 | 结果卡区双区外壳是 epic 新件；过渡期互斥先行、外壳落地后并存 | 本 epic |
| 3 | 工序图示 5 个受管动效资产需 `src/design/` 出（样张用 SVG 近似演示动作/时长）| `src/design/` |
| 4 | 英文片场话需二次创作，暂回落朴素版（`en` = "Generating…"，不假装做完）| §6.1 |
| 5 | 音频播放器/音色选择/转写分段随配音·TTS 落地；本文只占位不画 | §13/§17 |
| 6 | C8 图标白名单：实现班用任何新 Tabler 图标前先扩 `src/vendor/tablerIcons.ts`（样张不用未登记图标）| 实现班 |

---

## 10. 决策表（需用户拍板的分歧点 · ≤5 条）

见独立文件 `docs/design/2026-09-01-agent-ui-redesign-decisions.md`，摘要：

| # | 分歧点 | 现状 | 建议 | 理由（一句）|
|---|---|---|---|---|
| D1 | 工作方式轴：3 档掰回 vs 保留 4 档自主强度 | 过渡版契约级 4 档 `ask/guided/balanced/auto` + 丢 `编辑选中` | **掰回 §14.1 的 3 档 `Ask/编辑选中/Agent`** | §14 是用户 2026-08-27 亲裁的两根正交轴，4 档把「能碰什么」和「问多少」揉成一根、且丢了「只改冻结选区」这档 |
| D2 | 结果卡区落地节奏：过渡期先互斥 vs 直接上双区并存 | ③合流已拍板终局，但双区外壳未落地 | **过渡期先互斥（屏C）、外壳就绪后并存（屏D）** | 拆解卡订阅工件状态不绑面板布尔，两阶段都不返工；先互斥不阻塞拆解 v1 交付 |
| D3 | composer 附件触发：保留独立 📎 vs 并进「执行方式」 | 过渡版 📎 独立 | **保留 📎 独立（收簇后仍 5 簇，在预算内）** | 附件是高频输入动作、并进去反而多一次点击（§1.5.4 反例）；5 簇未超预算无需再收 |
| D4 | 付费富卡「本会话不再提示」：agent 对话档是否给这个开关 | `SpendConfirmDialog` 用户直发档有此开关 | **agent 受理档不给「不再提示」（每次必确认），仅用户直发档保留** | agent 自动规划时静默扣费风险高于用户亲手点，诚实优先（#194 §10 两宿主语义已分） |
| D5 | 开闸删旧范围：过渡版 4 档契约 + 互斥标志一次删净 vs 渐进 | 过渡版活着 | **开闸同 commit 删净（P1 加新必删旧、无并行版）** | 留着两套工作方式/两套右槽逻辑=并行版违反 P1；拆解 handoff 靠工件状态键平移不需渐进 |

---

## 11. 布局规格（v3.2 补全 · 数字单一真相源）

> 样张「屏 E · 布局」的全部数字落账处。三条总则：**① 一个家**——面板全页面停靠**视口右缘**，切页不挪窝；**② 一个宽度**——全页面共用 `assistantWidth`（承 §8.3，拆解表/结果卡区/浮窗回停同源）；**③ 让位不悬浮**——停靠态是 in-flow 列，主内容被挤窄不被盖（#194 §8.3「动作不压内容」的布局面）。

### 11.1 逐面落位（依据真实外壳，file:line 实扫）

| 面 | 面板落位 | 主内容让位 | 依据 |
|---|---|---|---|
| 创作 creation | 右缘全高（顶栏 56 下沿 → 窗底；无时间轴） | 居中网格（文稿列 240 + 编辑器，max 1480）在**内容区**（视口−面板）内居中；**取代**现网格内 344px 助手栏（P1 删旧，宽度归一 340） | `CreationWorkspace.tsx:59`（现 344 列）；「常驻右栏」拍板 2026-07-25 不变 |
| 分镜 storyboard | **与创作完全一致**（面板可用） | 同创作；中栏换成分镜表 | 分镜非独立页签：步进器只有三格、storyboard 亮在「创作」（`identity.tsx:137`）；同壳换中栏（`CreationWorkspace.tsx:72`） |
| 生成 generation | 右缘、**停在时间轴上沿**（时间轴 188 通栏在下；时间轴收起→面板到窗底） | 探索栏（60 轨/300 展开）+ 画布挤窄；画布底线 700 | `GenerationWorkspace.tsx:82-83`（面板列 + 时间轴行）；时间轴居底=用户拍板 2026-08-08 |
| 预览 preview | 右缘、停在剪辑时间轴（222 通栏、全密度不可收）上沿 | 素材源 268 + 播放器挤窄 | `PreviewWorkspace.tsx:35`；`tailwind.config.ts:104-105` |
| 浏览器打开 | **不并存**：对话框全屏接管（z-520；mac 从 y=0 连顶栏全盖、win32 从窗控条 32px 下起；不透明纸面，无压暗一说），网页是原生 WebContentsView 浮于一切 DOM 之上，右缘另有捕捞坞（默认 500、拖 220–75%）——面板不可见不可用，不装可用 | — | `NomiBrowserDialogView.tsx:120`；`NomiBrowserDialogModel.tsx:141,304`；`NomiBrowserDialog.tsx:555-557` |

**浏览器 → @ 回填**（素材桥，不走并屏）：悬停网页图/视频 Ctrl+C → 捕捞坞收进项目素材（现役链路）→ 关闭对话框后 composer `@` 选择器**置顶「刚从浏览器捞的」组**（本会话捕捞，含缩略图）。浏览期间 Agent 任务不中断、不弹通知；回来看顶栏角标蓝点/未读数。

### 11.2 缩小三档

| 档 | 规格 |
|---|---|
| 收起态 | 面板 → 顶栏角标：右簇「浏览器」与「设置」之间那格（= 现役 CollapsedAiChip 的家，`NomiAppBar.tsx:274`；win32 同位）；ghost 钮 30px 高；**蓝点 8px = 有新动静**（settle 420ms 单次脉冲）、**数字徽标 = 未读条数**（语法复刻 TaskCenterButton/CollapsedAiChip）；点角标原宽原状态还原（collapse≠unmount）。过渡期互斥角标与收起角标**同格只出一颗** |
| 窄窗态 | 顶栏 ≤1600 动作钮收 30px 图标方块（现役 `max-[1600px]`）；**面板宽度从不自动变**，只收拖宽上限：max = min(600, 视口−760)（<1360 起生效：1280→520、1200→440、1100→340）；760 = 探索栏最窄轨 60 + 画布底线 700；内容区断点（如创作文稿列 240→200）改按**内容区宽度**（视口−面板）评估 |
| 最小窗态 | **1100×720**（`electron/main.ts:295-296` 锁死；min-window 走查同尺寸 `tests/ux/creation-work-surfaces.e2e.mjs:226`）。实数：生成 60+700+340（面板高 476，时间轴收起 664）；创作/分镜内容区 760 → 文稿列 200 + 编辑器 ~490；预览 268+492+340。面板**仍可停靠，不强制收起**；放大钮置灰 |

### 11.3 面板操控三能力

| 能力 | 规格 | 理由 |
|---|---|---|
| 拖宽 | 左缘热区 **8px** 竖条骑边线（左右各 4px，≥6px 达标）· hover **col-resize** 光标 + 3×32px accent 竖粒 · 拖动实时、松手落库 · **min 300 / 默认 340 / max = min(600, 视口−760)** · **双击回 340** | 解剖整套复用现役生成区手柄（`GenerationWorkspace.tsx:157-173`）+ store clamp（`workbenchStore.ts:332`）——不发明第二种手柄、不立第二个宽度数 |
| 放大 | 面板头 ⤢ 钮：340（记忆宽）↔ 当前拖宽上限（宽屏即 600）一键切换；还原回放大前宽度；最小窗上限=340 → 钮置灰 + tooltip「窗口太窄」 | **不全屏盖**：产物全在画布（去画布/对照/勾镜），盖画布=蒙眼批准，违背结果卡区「对着改」的存在理由；600px 够拆解全表/计划卡完整可读。**不另做分屏态**：放大只是拖宽快捷键，全程只有 `assistantWidth` 一个变量，无第二种布局模式 |
| 拖走 | 面板头空白区抓拖（grab 光标）· 出槽阈值 **40px** → 浮动窗（默认 **380×620**，min **320×420**，位置尺寸记忆，全 app 限一扇，跨页跟随）· 拖回右缘 **30px** 热区亮停靠预览、松手吸附 · 头部「回停靠」钮一键归位 · 收起角标对浮动态同样生效 | **推荐浮动、否决换边**：三个面左缘全被占（探索栏 60/300、文稿列 240、素材源 268），靠左=一边叠两栏且各页左栏宽度不同、切页面板横跳，拆掉「一个家」；浮动有真用例（超宽/双屏画布全宽）且机构现成（捕捞坞同款 `useResizableFloatingWindow`）；浮窗盖节点是用户主动拖放的选择，不属「系统把动作压在内容上」 |

### 11.4 实现注记（开闸班用）

- 断点从视口 media query 改**容器/内容区宽度**判定（面板停靠让视口断点失真）。
- 收起角标 = CollapsedAiChip 泛化（从「生成+互斥」三条件放宽到「面板收起」一条件），同格互斥不双显。
- `@` 选择器「刚从浏览器捞的」组吃现役捕捞→入库事件，不新增采集通道。
- 浮动窗复用 `useResizableFloatingWindow`；回停写回 `assistantWidth`，不留第二份宽度。
- 创作侧删 344px 网格列时同 commit 移除其断点（`max-[1320px]` 堆叠分支），P1 无并行版。

---

## 附：来源索引（全绝对路径 / 编号）

- #194 规范：`docs/design/nomi-agent-interaction.md`（21 形态 §4 · 8 态 §2 · mode×approval §14 · 控件 §8 · 动效 §7 · 付费双宿主 §10 · 输入 §13 · 反馈 §17）
- 五源汇编：`docs/design/2026-08-31-agent-interaction-synthesis.draft.md`（S1-S20 硬规格 · 节3 自家复用件 file:line · 节4 AI Elements 对账 · 节5 Beautiful UI · 节6 18 条样张指导）
- 过渡版对账：`/Users/aoqimin/Desktop/Nomi-shell-mockup/RESIDENT-SHELL-DECISION.md`（21 形态 ✅10/🟡5/❌6 · C1-C8 · B档带条件收）+ `.mockup-shots/01-11.png`（11 张真机截图）
- M 线裁决：`docs/architecture/agent-m-line-rulings.md`（R-M-4 权限档位 · 12-15 语义工具）
- 右槽③合流：`docs/design/2026-09-01-right-slot-agent-workspace-opinion.md`（双区 · 结果卡契约 · 七维论证）+ `docs/plan/2026-09-01-video-deconstruction-v1.md` §4.1（R-C-1~7 互斥/并存契约）+ 视图 06/07
- 控件预算：`docs/design/nomi-design-system.md` §1.5（L1≤5 · 一功能一个家 · 先分组→去重→归位→收纳）
- QA：`docs/qa/2026-09-01-agent-m0-red-lights.md`（C9 侧：开闸删旧 + handoff 重接）
- 自家复用件（synthesis 节 3，全 `/Users/aoqimin/Desktop/Nomi/`）：`AgentPlanCard.tsx`(396) · `CommittedProposalCard.tsx`(137) · `ReconcileDeviationCard.tsx`(233) · `SpendConfirmDialog.tsx`(474) · `TaskCenterPanel.tsx`(422) · `AttachmentRail.tsx`(148) · `CardStackPeeks.tsx` · `CanvasSelectionToolbar.tsx`(108) · `agentUsageStore.ts` · `staleConversationDivider.tsx`
- **v2 库解剖源（2026-09-02 WebFetch 现役页面）**：Beautiful UI https://www.beautifului.dev/ （20 组件单页展柜：Loading/Thinking/Streaming Text/Approval Card/Tool Chips/Task Rows/Chat/Prompt Bar/…/Selection Actions）· AI Elements https://elements.ai-sdk.dev/ （Context/Checkpoint/Message/Plan/Queue/Artifact/… 组件 docs 子页）
- **v2 竞品证据（file:line，全在 `docs/research/2026-08-24-agent-workbench/`）**：LibTV.md:80-81(P83-84 流程分段)/:86(P90 批量范围单个·部分·全部)/:107(P116 先检查 shot 再批量) · LibTV_CLI.md:115(P98 失败是否扣积分不承诺)/:186-187(结果写回画布节点) · web/tapnow_brainstorm.full.dom.md:145(确认物落画布不翻长聊天记录)/:190(一次一个关键问题) · web/xyq_canvas_manual.full.dom.md:258(查看已存版本单入口) · 对账矩阵 `2026-08-24-agent-workbench-comparison.md` §3（19 项能力均无「有出入」形态）
