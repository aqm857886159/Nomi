# C71 根因返工验收

状态：真实闭环及人工画面检查完成；最终门岗/交付进行中。旧缩放重放截图和数值已删除，仅保留原真实八镜症状 `../c71-speed/gate-r2-timing.json`。

## 真实两镜

来源 `real/report.json`，真实 APIMart Grok Imagine1.5，480p、6秒×2、16:9。AI规划及两视频通过真实网络；没有注入计划或替换供应商产物。`buildProvenance` 记录实际运行的dirty build树与双产物SHA，不能把运行中新增类型收窄/门岗修订算成当时构建。

| 八站 | 实测秒 | 结果 |
|---|---:|---|
| 00 隔离空项目 | 3.375 | 通过 |
| 01 导入剧本 | 1.063 | 通过 |
| 02 真实AI规划、审批 | 31.747 | 工具写对1/1；回合成功1/1 |
| 03 物化视频节点 | 1.194 | 通过 |
| 04 供应商生成与落盘 | 58.699 | 两条POST200、两条本地视频 |
| 05 入轴播放 | 30.008失败；恢复5.145 | 脚本漏切生成画布；同项目恢复，不重复生成 |
| 06 导出完整解码 | 15.215 | 12.067秒MP4，16:9、2片段、ffmpeg完整解码 |
| 07 冷启动恢复 | 2.864 | 2视频资产与2片段一致 |

原05失败和恢复均保留，不能把相加结果冒称无中断端到端时长。另有 `real/first-attempt-failed.json`：审批后脚本先读到两镜中间态而提前退出，已改为等待完整方案契约；所有费用预约保留。

累计保守预约 **¥3.6706991843**，9次付费尝试（7文本，含第一次中断；2视频）；恢复阶段所有HTTP出站禁用，新增付费0。按live价预约上界，不是已核实账号账单。真实预算总账在 `/tmp/c71-real/real-budget-ledger.json`，不因重跑清零。

真实两镜的第7/8镜等待 **N/A**。旧真实八镜第7/8镜等待150.072/151.140秒仅是原始症状：没有同条件前跑，不报告加速比或节省秒数。

人工看图：规划后两镜已保存截图、入轴预览、导出首/中/尾帧；河边树与阳光→水面落叶，顺序正确，无人物/字幕/对白，产物非空。最终照片记录见 `real/first.png`、`middle.png`、`last.png`。

## 零费用八镜

`loopback/report.json`：00–07完整通过。服务端在全部八镜提交前不返回任何终态，八镜全部已提交才开始返回终态；额外本地生命周期排队 **0**。真实Electron交互、HTTP、资产本地化、入轴、64秒导出和冷恢复；颜色测试视频仅是协议产物，不注入供应商耗时、不作真实提速证据。

## 结构证据

- `hardcoded-red.json`：最终AST规则扫描旧HEAD，11处红（6文件）；`hardcoded-green.txt`：生产命中0。新增无白名单、不可更新基线。
- `grok-contract-red.txt`：旧APIMart时长范围30与官方15不符红；映射修为resolution并保留内部quality参数。
- `background-query-green.txt`：后台先读终态后前台仍取得产物；取消排队与真实终态释放回归。
- `provider-research.md`：APIMart/MiniMax/DeepSeek官方来源与未公开维度。
- `cross-pool-review-r2.json`：跨池原始意见，包含已接受和证据不成立的条目；裁决见方案补充，不把该原始意见冒称全通过。

## 最终交付验证

`final-gates.json`：完整 gates exit 0；77 契约、0 阻断、3 文档 advisory；Vitest 1328 文件 / 12124 测试通过，后续运行时测试与双构建通过。最终 loopback 已在 `687047e0beba` 加本任务工作区改动的构建上重跑，八站通过、64 秒导出完整解码、冷恢复通过；主代理看过 05/07 截图。构建后的变化仅为验证文档和证据。

`cross-pool-review-final.json`：最终边界复核无可证实阻断；审查范围和未验证项保留在原文。
