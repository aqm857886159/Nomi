# Round 5 · 达轮数上限，语义覆盖仍不稳定

基线8e9c17e92，同第3/4轮冻结输入和缩写文稿，创作/生成各5句。loopback先完成，随后官方DeepSeek。第5轮到顶停止，未达到连续两轮稳定阈值。

| 模式 | 工具写对率 | 分镜保存成功率 | 回合成功率 |
|---|---:|---:|---:|
| loopback | 10/10 = 100% | 10/10 = 100% | 10/10 = 100% |
| DeepSeek 官方 V4 Flash | 7/10 = 70% | 7/10 = 70% | 7/10 = 70% |

费用上界¥0.374534912；本单3轮共¥1.384852736，低于¥5，每轮低于¥1.5。零媒体生成，原生工具序列无付费生成工具。10次真实写调用包含1次拒绝和2次语义不符；#3没有工具。失败未删、没有改断言/提示词措辞。

## 严格失败判定

- #3：模型认为第3镜是修鞋手部特写、小禾不在画面，先询问是否改画面。未写，保存/回合失败。
- #4：首次patch漏modelVendor，现有共享schema正确拒绝；补齐code-newcli-com后成功。GPT Image 2默认1K生效，不声称显式写过resolution。
- #8：模型把小禾写进第3镜prompt，却未在该镜anchorIds加入人物锚he。工具返回成功，用户引用要求未实现，三指标均不能算成功。
- #9：2K落盘，但完整方案提交时顺手把he加到第3镜anchorIds，违反本句“其余镜头不动”。不能用修好上一句来抵消当前越界，工具写对/保存/回合均失败。

最后磁盘是4镜、2K、9:16、人物锚已挂第1/3/4镜；终态看起来正确不能反向洗掉#8/#9逐句错误。

## 聚簇与最早共享边界分析

前两轮的媒体配置、夹具参数错误、方案目标身份问题本轮未复现。剩余是“模型计划与用户局部意图不一致”：proposal输入已遗漏锚引用，而窄patch不支持anchorIds/params，模型转用完整propose后可顺带改其他镜头。共享目标hash防并发串写，但不携带用户原话中“仅第1镜”的结构化字段授权，因此无法据此拒绝语义越界。`canvasWrite.ts`明确限定窄patch字段、`canvasWriteTarget.ts`绑定目标、`proposalTxn.ts`核验执行与已批准参数一致；这三处没有把自然语言推断伪装成强约束。

先查别人：沿用此前实查DeepSeek工具调用与pi原生isError回流；本轮身份拒绝可自动恢复。Zod.refine已覆盖身份成对要求，不能取消校验。后续需要研究最早用户意图→字段作用域→批准写入之间的结构化约束，补真实模型回归；仅提示词提醒或从历史对话猜目标不算根因修复。本单轮数已到，不追加未验证生产改动，不宣称这类产品可靠性已解决。

## 交付

每句原生转录、前后落盘、截图与严格trace已封存。第3/4轮新增回归2/2绿，最终完整gates见LOOP-LAST.md。#698在执行期间被合并，后续改动按任务书转到最新origin/main上的新分支/新PR；本轮真实行为数字对应上述基线，不冒称新合并UI上的额外真实轮次。
