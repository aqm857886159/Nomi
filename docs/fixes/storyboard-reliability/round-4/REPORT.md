# Round 4 · 完整保存与可恢复的身份遗漏

基线 bb5eaa915，输入及同一合成文稿与第3轮一致。新建隔离profile，loopback后官方DeepSeek，创作/生成各5句。零媒体生成。

| 模式 | 工具写对率 | 分镜保存成功率 | 回合成功率 |
|---|---:|---:|---:|
| loopback | 10/10 = 100% | 10/10 = 100% | 10/10 = 100% |
| DeepSeek 官方 V4 Flash | 10/11 = 90.9% | 10/10 = 100% | 10/10 = 100% |

费用上界 ¥0.467599488，本单累计 ¥1.010317824。所有10句均真实写入；首败计入工具分母，不以重试抹去。#4第一次patch只给modelKey，Zod在共享`canvasWrite.ts:241`以`modelKey and modelVendor must be given together`拒绝；模型读取上下文后补modelVendor=apimart重试成功。该句切换到GPT Image 2后使用真实档案默认resolution=1K；没有显式resolution字段，不把“未写字段”说成“已写字段”。#9显式2K落盘，未改其他镜头；#10全镜9:16落盘。

## 聚簇、先查别人和边界裁决

- 配置/夹具档位/方案身份簇均未复现；第3轮的no-op本轮没有出现。
- 模型遗漏复合身份：这是被现有共享校验拦住的无效请求，自动纠正后闭环。查`canvasWrite.ts`的设计说明和`canvasWrite.test.ts`的model-only拒绝回归：模型身份必须成对；扁平schema是为了Google OpenAPI参数兼容，组合限制留在校验层。与已实查DeepSeek Tool Calls、pi的isError回流机制一致。不能用取消供应商校验、供应商分支或提示词换词来冲绿。本轮无新增生产补丁，保留错误与恢复的真实数字。
- narrow patch不承载resolution是现有刻意限定的字段面；#9用完整方案写入成功且同一身份保护生效，没有证据要求扩大写权限。没有将有限patch工具改造成任意对象写入。

## 证据与退出

每句原生JSONL、实际磁盘前后快照、截图已无损封存，未执行媒体。第3轮补充冷启动后真实方案截图（同一个隔离项目、只读观察）随本轮收录；原先截图过早捕获加载态的尝试不作验收证据。第4轮只有一轮满足阈值，进入第5轮复扫。完整gates最终见LOOP-LAST.md。
