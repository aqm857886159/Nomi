{
  "schema_version": 3,
  "id": "2026-09-05-mcp-canvas-write-durable-receipt",
  "problem_type": "MCP canvas write durability and cancellation contract gap",
  "symptom": "A real nomi_canvas_edit canvas.write could mutate project.json and return applied:true without a main-process durable proposal receipt, so the effect had no queryable committed evidence and no restart recovery proof.",
  "direct_cause": "The headless stdio invoker wrapped document.write in the shared receipt lifecycle but dispatched canvas.write directly; canvas results also minted their proposalId inside dispatcher.ts, leaving no transport-owned prepare/commit boundary.",
  "class_root": "Semantic MCP writes crossed the catalog, lease, dispatcher, and disk gateway as independent effects, while receipt ownership and request identity were implemented only for one method. Without a common main-process lifecycle, successful writes and cancellation/error paths could not share durable evidence or replay protection.",
  "migration": "Existing projects remain readable. New canvas.write calls through the production stdio invoker prepare and commit the existing .nomi/project-agent-proposal-receipt.json journal, reuse the committed binding after restart, and fail closed on duplicate request identity, schema, lease, disk, or cancellation errors. GUI renderer journey certification remains outside this slice.",
  "affected_population": [
    "MCP clients using nomi_canvas_edit against a headless Electron stdio process",
    "Projects whose canvas mutation succeeded before the process exited or was restarted",
    "Cancelled, malformed, unauthorized, or failed-disk canvas writes that previously had no shared receipt boundary"
  ],
  "scope_paths": [
    "electron/capabilityCore/mcpDocumentWriteReceipt.ts",
    "electron/capabilityCore/mcpStdioServer.ts",
    "electron/capabilityCore/dispatcher.ts",
    "electron/capabilityCore/core.ts",
    "electron/capabilityCore/gateway.ts",
    "electron/capabilityCore/mcpProtocol.ts",
    "electron/capabilityCore/mcpLoopbackRpcRequest.ts",
    "electron/capabilityCore/mcpNodeLauncher.ts",
    "electron/capabilityCore/rpcServer.ts",
    "electron/projectAgentHost/projectAgentProposalReceiptStore.ts",
    "electron/shared/projectAgentProposalReceipt.ts"
  ],
  "entry_points": [
    "MCP_CAPABILITY_RESOLVER.resolve('nomi_canvas_edit') → canvas.write",
    "createMcpStdioDirectInvoker → dispatchAndEnrich → dispatcher.ts canvas.write",
    "ProjectAgentProposalReceiptService at .nomi/project-agent-proposal-receipt.json"
  ],
  "invariants": [
    "A real canvas.write commits a main-process durable receipt only after the disk gateway returns an applied result, and the result proposalId equals the committed receipt proposalId.",
    "The receipt binding is checked against the verified project lease and survives a fresh receipt-service instance after restart.",
    "A repeated request identity cannot apply a second disk mutation; malformed schema, invalid lease, failed disk read, and pre-effect cancellation leave the canvas unchanged and never produce a committed receipt.",
    "After an effect, late cancellation, partial application, or receipt commit failure is never represented as undone; the durable lifecycle is effect_unknown, partial, or commit_failed and preparing evidence is reconciled after restart.",
    "A JSON-RPC request id is carried through handleIncoming, the stdio/loopback transport, and the existing receipt owner; same-id same-parameter calls replay while same-id different-parameter calls conflict deterministically.",
    "The production test enters through the catalog resolver and exercises the real lease, dispatcher, and disk gateway; old patch_shots probes and injected stores are not evidence for this contract."
  ],
  "regression_tests": [
    "electron/capabilityCore/mcpDocumentWriteReceipt.test.ts",
    "electron/capabilityCore/mcpStdioCanvasWriteReceipt.test.ts",
    "electron/capabilityCore/mcpLoopbackRpcRequest.test.ts",
    "tests/ux/mcp-canvas-write-durable-receipt.e2e.mjs"
  ],
  "residual_risks": [
    "The canonical patch_shots renderer-owned journey is not certified in this PR; it remains bounded by its existing renderer receipt owner and is not used as evidence for ordinary canvas.write.",
    "An abort arriving after a synchronous disk gateway has already applied cannot undo that physical write; the durable receipt records effect_unknown and restart reconciliation keeps the state truthful."
  ],
  "external_sources": [],
  "internal_only_reason": "This is an internal Nomi receipt, lease, filesystem durability, and MCP cancellation contract; no external protocol or provider documentation can establish the trusted project binding or local recovery semantics.",
  "recurrence": {
    "classification": "one_off",
    "reason": "The known missing production edge is the canvas.write transport seam adjacent to the already-existing document.write receipt seam. The shared helper and regression journey close this bounded M0-M5 slice; a broader recurring prevention contract would require a separately scoped audit of every renderer-owned write surface.",
    "same_class_scan": [
      "Read document.write receipt helper/tests and rpcServer.ts: document.write already has a prepare/commit/undone lifecycle; canvas.write was the uncovered semantic MCP sibling.",
      "Read canvas.write catalog, resolver, dispatcher, gateway, and existing patch_shots journey: the old storyboard evidence exercises a different renderer-owned path and is intentionally not reused as canvas.write proof.",
      "Read ProjectAgentProposalReceiptService: its binding, CAS, operation replay, atomic file write, fsync, and recovery lifecycle are reused rather than copied into a second store."
    ]
  },
  "legacy_paths": {
    "status": "not-applicable",
    "removed_paths": [],
    "rationale": "There is no separate legacy canvas receipt store to delete; the pre-existing direct canvas.write dispatch remains the domain effect and is now enclosed by the shared main-process receipt boundary."
  },
  "dependency_lifecycle": {
    "decision": "not-applicable",
    "rationale": "The change reuses existing Node, Vitest, Electron, and receipt-store dependencies without adding or upgrading a dependency.",
    "exit_criteria": []
  }
}
