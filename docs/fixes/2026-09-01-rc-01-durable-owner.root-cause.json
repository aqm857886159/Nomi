{
  "schema_version": 3,
  "id": "2026-09-01-rc-01-durable-owner",
  "problem_type": "Agent conversation and control state has multiple durable-looking owners",
  "symptom": "Long turns, renderer remounts, or restart can replay prompt text without the corresponding structured item/receipt and can repeat an effect.",
  "direct_cause": "ProjectAgentHost records state while the coordinator forces history to ephemeral, executionPrompt rebuilds full text, and each Pi turn can create a fresh session.",
  "class_root": "No single durable owner publishes Thread/Turn/Item/Checkpoint state and binds loop output to an append-only ledger.",
  "affected_population": [
    "Every Agent thread crossing a turn boundary",
    "Every renderer remount, Electron restart, interrupt, or resume",
    "Every tool call with a receipt or external effect"
  ],
  "scope_paths": [
    "electron/projectAgentHost/projectAgentHost.ts",
    "electron/projectAgentHost/hostLifecycle.test.ts",
    "electron/projectAgentHost/projectAgentHost.ts",
    "electron/projectAgentHost/projectAgentExecutionCoordinator.ts",
    "electron/ai/agentChatV2Ipc.ts",
    "electron/capabilityCore/canvasReadSurfaceIpc.ts",
    "electron/capabilityCore/projectLeaseStore.ts",
    "electron/projectAgentHost/projectAgentIpc.ts",
    "electron/projectAgentHost/projectAgentProposalReceiptStore.ts",
    "electron/projectAgentHost/projectAgentRepository.ts",
    "electron/skills/skillIpc.ts",
    "electron/skills/skillStore.ts",
    "electron/workspace/workspaceRepository.ts"
  ],
  "entry_points": [
    "ProjectAgentHost execution coordinator",
    "Pi runtime turn adapter",
    "Renderer IPC resume/remount"
  ],
  "external_sources": [],
  "internal_only_reason": "This is an application ownership invariant; the exact #223 source is pinned by the M0 owner map and no external dependency decides it.",
  "invariants": [
    "A Thread/Turn/Item/Checkpoint has one durable Host owner.",
    "Pi sessions are replaceable loop implementations and never project facts into a second history.",
    "A settled idempotency key cannot dispatch a second effect."
  ],
  "generality_proof": "All checked callers converge on ProjectAgentHost dispatch/coordinator and then the Pi adapter; enforcing ledger append, sequence, lease, and receipt idempotency there covers renderer remounts, restarts, retries, and future loop implementations.",
  "shared_boundaries": [
    {
      "path": "electron/projectAgentHost/projectAgentHost.ts",
      "symbol": "ProjectAgentHost",
      "responsibility": "Owns the durable Host ledger and lifecycle barrier."
    }
  ],
  "same_class_entry_points": [
    {
      "path": "electron/projectAgentHost/projectAgentHost.ts",
      "entry_point": "acceptIntent",
      "disposition": "enforced",
      "evidence": "All turns converge on the Host ledger."
    },
    {
      "path": "electron/projectAgentHost/hostLifecycle.test.ts",
      "entry_point": "restart-and-replay",
      "disposition": "enforced",
      "evidence": "Restart regression exercises the same durable boundary."
    }
  ],
  "recurrence": {
    "classification": "recurring",
    "reason": "Any alternate caller, loop implementation, restart, or future persistence adapter can recreate split ownership if the Host boundary is not structural.",
    "same_class_scan": [
      "Searched #223 coordinator, helpers, Host, IPC, and Pi runtime refs for history/ executionPrompt/session creation.",
      "Checked renderer remount and resume as separate callers of Host IPC.",
      "Checked tool result/receipt paths for idempotency and terminal item writes."
    ]
  },
  "prevention": {
    "kind": "centralized-boundary",
      "enforcement_path": "electron/projectAgentHost/projectAgentHost.ts",
    "invariant": "Only Host ledger append/reducer can create terminal conversation or execution state; Pi output is an input item, never a second history.",
    "failure_mode": "Stale lease/revision, duplicate idempotency key, malformed sequence, or incomplete settlement is rejected or remains non-terminal.",
    "exception_policy": "none",
    "strategy": "Replace ephemeral replay with Host-owned Thread/Turn/Item/Checkpoint and make every loop adapter return typed items into the same reducer.",
    "artifacts": [
      "electron/projectAgentHost/projectAgentHost.ts",
      "electron/projectAgentHost/hostLifecycle.test.ts",
      "electron/projectAgentHost/projectAgentHost.ts",
      "electron/projectAgentHost/projectAgentExecutionCoordinator.ts"
    ]
  },
  "regression_tests": [
    "electron/projectAgentHost/hostLifecycle.test.ts"
  ],
  "class_regression_tests": [
    "electron/projectAgentHost/hostLifecycle.test.ts"
  ],
  "legacy_paths": {
    "status": "removed",
    "removed_paths": [
      "src/workbench/ai/agentLoopMode.ts",
      "src/workbench/capability/capabilityApplyHandler.ts",
      "src/workbench/generationCanvas/agent/generationCanvasAgentClient.ts",
      "src/workbench/generationCanvas/agent/runStoryboardPlanner.ts"
    ],
    "rationale": "Production callers now use durable bindings and Host settlement."
  },
  "dependency_lifecycle": {
    "decision": "not-applicable",
    "rationale": "The missing invariant is Nomi Host ownership, not a third-party runtime defect.",
    "exit_criteria": []
  },
  "migration": "Replay existing durable Host items by threadId and seq; do not silently import full prompt text. Unmatched legacy sessions remain visibly non-resumable until an explicit migration decision.",
  "residual_risks": [
    "The exact maintainer comment on PR #272 is unavailable, so any exception to this owner boundary remains OPEN QUESTION.",
    "M1 must prove on-disk persistence and process-restart behavior before this draft can become an implementation contract."
  ]
}
