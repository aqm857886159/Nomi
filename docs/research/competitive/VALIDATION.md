# 流程验收记录

> 状态：2026-09-19 流程静态验证与桌面演练通过；真实竞品研究未执行，定时首轮未运行。

## 检查范围

- `skill-creator/scripts/quick_validate.py`：Skill is valid。
- `node scripts/check-skills-format.mjs`：创作者技能 88/88、研发宿主技能 2/2，pi 真实加载器零 diagnostics。
- 技能与 competitive 目录中的本地 Markdown 引用：5 个文件，失效目标 0。
- 本机自动化：TOML 可解析，kind=cron、ACTIVE、主仓 cwd 有效，DTSTART 显式固定 Asia/Shanghai 的 2026-09-22 10:00，RRULE 间隔 3 天。这里只证明配置已准备，调度器接纳与首次执行须另看本机运行记录；不把配置测试当完整周期。
- 本机发现入口：`~/.agents/skills/nomi-competitive-radar` 链接本任务 worktree 的技能正本。PR 合入并清理该 worktree 前，须改指稳定主仓中同路径；不复制技能正文。

## 独立六视角审阅

按仓库 R7，由独立审阅者只读检查：CTO 看复用/调度/恢复，设计看页面/图标/引导与因果证据，PM 看优先级与范围，前端看真实交互与异常态，后端看失败分类/证据留存，真实用户看同任务对照和首次成功。覆盖范围与“只落流程”边界通过。

提出并修正三条：

1. 实际开始时间加 72h 会使固定调度跳轮：改固定 DTSTART/计划锚点，离线补跑不移动锚点。
2. TikHub 固定文件名会覆盖别名/重试证据：改 query_id/attempt-NN 独立目录，重试只查失败平台，成功记录不覆盖。
3. 锁外读状态可串行重复执行、早期中断无状态：改锁内复核与固定 checkpoint.md，开始请求前写运行身份，每个查询完成后保存检查点。

## 桌面演练（非实测）

| 情境 | 预期行为 | 审查结论 |
|---|---|---|
| 正常周期 | 五家来源扫描，两家核心旅程、扩展轮换、两条内容卡，Nomi 对照后给实验建议 | 规程及模板覆盖，真实效果留首轮验证 |
| TikHub 退出码 0 但 X 失败 | 检查 failures，周期 partial，独立重试目录仅查 X | 不把成功退出码当全平台成功 |
| 登录失效/无录屏工具 | 继续可访问来源；记录受阻入口，截图序列只能 partial | 不伪造交互验收，不推断无变化 |
| 两个触发器先后进入 | 原子取锁，锁内再读 checkpoint/报告/PR | 第二个执行者不能按旧到期判断重抓 |
| 原始查询完成后中断 | 保存证据再标 done；重启读 checkpoint，未标完成项用新尝试目录 | 不覆盖证据，不重复整轮 |
| 计划日后两天才补跑 | 仍属于原计划周期，下个计划日不移动 | 固定三日节奏与来源观察窗分离 |
| 研究建议出现产品改动 | 仅关联 TODO/设计实验；不开发、不公开发帖、不自动合并 | 与本轮授权范围一致 |

## 尚未验证

五家账号/来源身份、TikHub 当前凭据/额度、真实鼠标录屏链路、Nomi 同任务对照、调度器首次触发及报告质量。以上要在首轮执行按来源分别记结果，不能用本文件代替。
