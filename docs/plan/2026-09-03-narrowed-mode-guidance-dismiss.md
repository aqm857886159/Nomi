# 收窄模式提示关闭

状态：🚧 进行中

## 范围

- 给 `NarrowedModeGuidanceNote` 增加克制的关闭按钮，按钮文字与 aria-label 走 `generationCommon` 的中英文 i18n。
- 在 `narrowedModeGuidance` 统一读取节点 meta 的关闭标记；消费点只把关闭动作写回当前节点 meta。
- 走查覆盖：当前节点关闭、另一节点仍显示、项目保存后重新打开仍关闭。

## 不动

- 不改变收窄判据、提示位置、浮层、层级或 z-index。
- 不支持跨节点、全局或组件 state 的关闭记忆。

## 验收与回滚

- 先跑新增断言的红测，再实现后跑绿测、指定走查和用户要求的三条验证命令。
- 回滚只需移除本次 meta 标记、关闭按钮及对应断言/i18n，不触碰既有供应商切换路径。
