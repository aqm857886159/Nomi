# 分镜表 v6 五条反馈返工执行计划

> 状态：🚧 进行中；用户已明确授权本轮返工，限定现有 worktree、分支、PR #518。

范围按 [反馈记录](../design/2026-09-06-storyboard-v6-feedback.md) 五条推进。顺序为删除重复与剪辑字段 → 固定参数网格 → 参考堆几何 → 场与全片播放 → 剧本来源设计。每条同步合同和实验室登记，分五个 commit 推送同一分支。

不动项：不新建 worktree/PR；不合并 PR；不接第 5 条的真实文稿读写；不改视觉基线、hooks、权限与付费生成链；保留 5186 vite。

回滚：对已推送的每个里程碑使用普通 revert，不重写分支历史。生产数据不主动批量重写；已存剪辑字幕/转场继续由剪辑面维护。

验收：删除路径结构与数据投影测试；参数布局纯函数；参考堆 1–30 张几何；播放清单纯函数与真实媒体切镜旅程；实验室注册、i18n、重新走查和接触表；最终 `pnpm run gates`，对旧视觉基线冲突如实报告并保持阻断。
