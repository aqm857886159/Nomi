# 时间轴 P0 与默认状态根因修复

> 状态：🚧 进行中

## 范围

- 将媒体片段移动、微调、复制和文字片段新增/移动统一到最近合法空位策略。
- 时间轴初次打开和素材时长变化时按可视内容宽度自动 fit，保留 8% 余量。
- 让轨道区域在轨道内容超过面板高度时可滚动，避免文字轨被父面板裁切。
- 文字轨和字幕入口在已有文字片段时仍持续可见；新增字幕避开已有文字片段。
- 增加对应单测、布局契约测试和零额度 Electron 走查证据。

## 不动项

- 转场入口、片段属性面板、右键菜单、AI 相关目录、Agent Host、创建/分镜流程、CI workflow、check 脚本。
- 不改变视觉 token 或做视觉重设计。

## 回滚

- 回滚本分支提交即可恢复原时间轴落位、缩放、轨道溢出和字幕入口行为；不改持久化 schema。

## 验收门

- 四条问题各有修前红、修后绿测试。
- `check:root-cause-contracts`、`check:tokens`、`check:heavy-path`、`check:boundaries`、`pnpm run gates` 全绿。
- 真实 Electron 走查覆盖相邻片段微调、默认 fit/时长变化、文字轨可见、字幕不重叠和入口持续存在，截图写入 `.tmp/`。
