# PR #619 · 四画板对账与 R13 补验

> 状态：🚧 进行中；尚未通过 R13 或实验室基线，不可据此合并。

## 范围与验收

仅 #619 找参考功能：核对 2026-09-09 handoff 四画板，补真实 Electron 旅程、实验室四态和视觉基线，修差异后 gates、更新原 PR。真实检索最多 3 次；key 只读环境变量，不输出；仅隔离资料库。保留既有走查断言与超时。最终合并由主会话/脚本执行。

## 先查别人

- 已有实现：`src/workbench/assets/FindReferencePanel.tsx:97` 把链接交给 `onShareLink`，关键词交给 `searchReferences`；不存在链接找相似素材的数据路径。复用这两条链，不凭截图宣称第三种能力。
- 已有走查：`tests/ux/find-reference.walk.mjs:112` 搜「护肤精华」，后续加入一条素材；`docs/qa/2026-09-08-find-reference-walkthrough.md` 记录用户否定推开网格、改为接管素材区的裁决。
- 已有实验室：`src/devlab/designLab/catalogLiveness/states/01-listing.tsx:16` 的宿主状态驱动真实组件；`tests/ux/design-lab/design-lab.visual.spec.mjs:30` 校验活页面注册表并用 Playwright 原生截图比较。复用注册表/取景/基线管线，不复制产品 UI。
- 官方契约来源：<https://docs.tikhub.io>、<https://github.com/TikHub/TikHub-API-Python-SDK>；本轮仅复核仓内已实调端点 `electron/connectors/tikhubConnector.ts:510`，无新 SDK/外部协议选型。

## 四画板逐项对账（初查，待真机验证）

真源：`~/Desktop/Nomi-handoff-20260909/design/find-reference.canvas.html` 与同目录 `find-reference-extracted/*.dc.html`。

| 画板 | 实现 | 差异 / 处理状态 |
|---|---|---|
| Main · 素材库外壳 | `AssetLibraryPanel.tsx:638`、`FindReferenceSection.tsx:48` | 画板保留底部原素材网格；现役接管素材区+返回条。仓内 09-08 走查记录这是用户明确修正，需明确 handoff 是否要求撤销该裁决。 |
| Main · 平台 chip | `AssetLibraryPanel.tsx:353`、`FindReferencePanel.tsx:151` | 三平台与高亮已有；平台只存 React 会话态，画板要求项目设定。 |
| Main · 关键词与转译 | `FindReferencePanel.tsx:99`、`electron/connectors/tikhubConnector.ts:495` | 链接导入/关键词检索已有；转译回显只存在契约，无实际翻译接线。 |
| Main · 筛选 | `electron/connectors/tikhubConnector.ts:501` | 请求支持 countryCode/objective，但 UI 没有画板的地区、卖货/推 App/流量或带货/科普/日常 chips。后者没有现成等价上游参数，不可伪装筛选已生效。 |
| Main · 结果与提示 | `FindReferencePanel.tsx:256` | 三列卡、加入/原片出口已有；没有画板按平台解释数据边界的提示。 |
| EmptyEntry | `AssetLibraryPanel.tsx:706` | 图标、空态、平台文案已有；画板就地输入+价格+换平台，现役为 CTA 后展开面板，多一次点击。 |
| PlatformEvidence | `electron/connectors/referenceSearch.ts`、`FindReferencePanel.tsx:294` | 归一证据按平台排序：抖音点赞/收藏，小红书收藏/点赞，TikTok 点赞/放量/CTR；需用真实组件四态及截图验其视觉。 |
| FailStates | `FindReferencePanel.tsx:223` | 空结果三段式+半年/换词已有；转译未接，不能到达完整获批状态；HTTP 错误仍 toast，需真实失败态截图。 |

## 真实任务与范围歧义

任务书「贴一个抖音链接找 3 条参考」与现役/画板「贴链接＝已知道要哪条、输关键词＝还不知道」不一致。已询问是链接导入后同主题关键词找 3 条，还是新增链接相似检索。未收到答复前不擅自扩建相似检索。

## 基线与回滚

开工从 PR 实际分支 `claude/tiktok-video-analysis-research-af7656` 创建本地任务分支 `claude/tiktok-video-analysis-research`。已按任务书并 main，6 处冲突保留原找参考功能并吸收 main 的 inline 通知、动效 token 与锁门岗。回滚用本轮任务提交的 revert；不重写远端、不动真实资料库。

## 验证记录

- `pnpm install`：成功；并 main 后重新安装新增依赖。
- `pnpm run delivery:preflight`：成功，main 基线 `6afb88ae85c62ab512d3a0c407f11b71aa073642`。
- `pnpm run build`：成功（并 main 后依赖重装，renderer 与 Electron 均成功）。
- 原样 `node tests/ux/find-reference.walk.mjs`：成功，1 次真实搜索，7 条抖音结果、7/7 封面绘制、1 条实际入库并返回可见，控制台零错误。它验证的是关键词找参考，不是任务书的链接找 3 条。
- 实验室 `find-reference`：4 状态注册，沿用生产组件与真实响应归一器；`--grep 'design lab · find-reference'` 录图 5/5、禁更新复验 5/5；结构检查通过；`tsc -p tsconfig.app.json` 通过。
- 新图是当前实现的回归快照，**不是四画板已对齐的证明**。差异仍以本表为准，完整验收未通过。
- `python3 scripts/with-gates-lock.py -- pnpm run gates`：已启动，正在排队，不抢锁、不删锁。


## 新增阻塞证据

- `tests/ux/pr619-reference-task.walk.mjs` 已走到真实贴链接入口，但 UI 报 TikHub 预期外数据，60 秒仍 0 条素材，截图见 `docs/evidence/pr619-finish/task/failure.png`。此任务失败；没执行后续关键词搜索/加入三条。
- 累计真实关键词检索 1 次、另 1 次贴链接解析操作；内部请求数未插桩，不编总 HTTP 次数。
- `check:test-waits` 红：原有走查 7 处固定 timeout 与最新 main 的预算门岗冲突，新诊断脚本 4 处；遵守禁令，未改断言或超时换绿。
- 截图对账与失败证据已可审阅；不能把实验室回归快照绿当成任务完成。


## 完整 gates 第一轮与整合修复

取得共享锁后完整 contracts 跑完，5 项红：门岗链（合并漏 icon-semantics）、filesize（AssetLibraryPanel 808 行）、dangling token（已退役 transition-fast）、test-waits（固定超时）、vocabularies（新 specimen 复制平台/展示词表）。其余包括完整实验室视觉、lint、typecheck 通过；contracts 红导致 unit/build 后段未运行。

修复范围仅本任务：恢复 icon-semantics 入链；迁移现役 duration-nomi-fast/ease-nomi-fast；重复外壳注释压缩并指回 FindReferenceSection；specimen 复用 REFERENCE_PLATFORMS，显示组合用布尔 props。前三类在共享静态门岗复验通过，vocabularies 通过；四态禁更新复验仍 5/5。固定超时仍不改。

根因：main 的门岗/动效词汇演进后，分支整合没有同时迁移相同消费点。已实扫 AssetLibraryPanelParts 与 FindReferencePanel（前者在首次合并迁过，后者遗漏），由既有 check:gates-chain/check:dangling-tokens/check:filesize/check:vocabularies 共同拦回归；无新依赖或新运行时边界。

## 第二轮：共享等待预算迁移

范围：合最新 main；迁移两个找参考走查的 11 处私有预算；完整持锁 gates、正常提交推送并更新 PR 收据。保留四画板差异与失败记录，不以门岗绿替代产品验收。回滚为本轮提交的 revert。

根因分类 recurring（测试消费点）：症状是 check:test-waits 11 处红；直接原因是脚本使用固定 timeout；类根因是新增走查未接共享 station 预算。实扫两个入口 find-reference.walk.mjs（7 处）和 pr619-reference-task.walk.mjs（4 处），全仓扫描没有其他新增违规。共享 owner 已存在于 tests/ux/_station-budget.mjs 与 scripts/check-test-waits.mjs，复用而不另造等待实现。本轮无生产改动、无依赖变更，不新增生产修复合同。

迁移保留全部 predicate/matcher：本地 DOM 单步使用 stationTimeout()（15s，不放宽旧 20s）；搜索整链使用四操作预算（60s，保持原上限）；下载及入库使用双操作预算（30s，保持原上限）。链接解析到落盘仍保留整链 60s。诊断文字不再谎称 20 秒。验证先记录原门岗红，迁移后跑门岗及共享预算/断言测试，最后完整 gates；不更新截图基线换绿。

## 最新 main 整合后的竞态夹具修正

完整 gates 合入 f68cf2c7b73d 后出现单测红：canvasWriteTarget.integration.test.ts 的 receipt preparation race 期待 target_stale，实得 receipt_unresolved；独跑可复现。临时诊断（已移除）定位到 onPrepare → addStoryboardDesign → ensureStoryboardShotTable → addNode → 写锁 AbortError，尚未走到 beforeApply 的目标校验。

分类 recurring（测试夹具语义漂移）：新增分镜已变成包含画布投影的写动作，不能再充当纯选择切换。实扫同文件 selection/content admission 用例及 receipt preparation race：前者在准备请求之后、取得写锁之前新增分镜，仍测试真实陈旧批准；只有后者要在锁内切换选择，因此应提前创建第二份，再在 onPrepare 调用 setActiveStoryboardId。共享边界仍是现役 beforeApply + captureStoryboardTarget，不改生产逻辑、错误码、原 matcher、内容断言或超时。回归跑整个 canvasWriteTarget integration 文件及相邻 unit，再完整持锁 gates。
