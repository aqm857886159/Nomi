# R13 走查暴露的五项缺陷修复计划（2026-09-03）

## 目标

修复供应商连接状态裸键、动态 i18n 门岗漏检、J06 历史 ETA 未到确认卡、供应商自动切换 toast 重复，以及验证本机 ComfyUI H3 工作流识别的走查欠账。

## 根因与范围

- **#1/#2**：状态 union 有四个成员，但连接词条缺成员，且动态前缀门岗只验证前缀、不验证成员。补齐中英文词条；把成员来源登记到共享状态常量，并让门岗逐成员校验。
- **#3**：从事件样本、主进程 IPC、bridge 到 `describeGenerationCost` 逐层核对，修复最早共享边界的历史统计传递问题，并保留冷启动估算的明确语气。
- **#4**：枚举所有供应商断开自愈 effect 的调用者，确认重复通知的共享触发边界；以一次状态迁移只产生一次 toast 为不变量。
- **#5**：仅使用真实 `~/ComfyUI` 目录和本机服务；启动失败如实记录，不伪造视频识别证据。服务验证完成后停止 8188。

## 不动项

不新增控件，不改布局，不扩大供应商或模型范围；保留基线已有的未提交文件与 `tests/ux/screenshots/` 未跟踪目录。

## 回滚与验收

每项独立提交，可逐项 `git revert`。#1/#3/#4 修完后重跑真实走查并保存 `walkfix-*` 截图；#2 先删除一条译文确认门岗红并点名 key，再恢复转绿；最后并线最新 `origin/main`，运行 `pnpm run gates`，仅推任务分支并创建一个未合并 PR。
