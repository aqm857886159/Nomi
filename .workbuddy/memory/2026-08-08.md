2026-08-08
- 调查并恢复创作页「Nomi 创作」胶囊定位：
  - 历史最早（`3c2fe821`）胶囊 className 为 `absolute top-[22px] right-6 z-[20]`。
  - 当前 HEAD（`545ebe0a`）被改成 `right-[-72px]`，已恢复为 `right-6`。
  - Playwright 在 1440x900 视口截图验证：胶囊 `right=1146`，创作区 section 右缘约 1170，间距正好 24px，完整在视口内，未与编辑器重叠。
  - 生成页胶囊未改动；本次未 push，仅本地修改。
- 清理 main 胶囊来回改提交：git reset --hard 77ee1b1e，删除 806c657d 之后 17 个胶囊 fix/revert 提交（75e54466..545ebe0a），保留飞书二轮 806c657d 及之前有效改动（八连修/工作区修复/素材库拖宽/时间轴居中/模型浮卡/快捷键溢出）。本地未提交的 right-6 改动随 reset 丢弃。
- 删除本地备份分支 backup/main-pre-capsule-cleanup-20260808（HEAD 原 545ebe0a），因用户要求"全部删掉"且 main 已干净。现本地 main 已无任何胶囊提交。Nomi-cap worktree(7ef83e27) 与胶囊无关，是 token /alpha 治理 Issue #32，不动。
- 用户确认要把远程也清掉：git fetch origin 后用 git push --force-with-lease origin main，将 origin/main 从 545ebe0a 强制更新到 77ee1b1e。验证：本地与远程 HEAD 均为 77ee1b1e，git branch --all --contains 545ebe0a 为空，808c657d..HEAD 无胶囊提交。胶囊来回改历史已本地+远程双清。
- 用户要求恢复历史 commit 中的两个特定改动：
  - 生成页 AI 胶囊从 `bottom-4 right-4` 恢复为 `top-4 right-4`（右上角）。
  - 画布键盘操作帮助面板由 `right-0` 向左展开恢复为居中（`left-1/2 -translate-x-1/2`），保留 `w-[30rem]`。
  - Playwright 实测：生成胶囊 `x:1314, y:72, right:1424`（右上角）；面板 `x:118, right:598`，左栏右缘 `60`，`PANEL_LEFT >= SIDEBAR_RIGHT: true`，未遮挡。
  - 同步更新 `canvasControlsStructure.test.ts` 断言，vitest 全量通过（435 files passed / 1 skipped）。
- 发布 v0.19.0：提交 3d08021d（UI fix）+ 916f3afa（版本 bump/CHANGELOG/营销站），打 tag v0.19.0，push main + tag 到 origin。
- 用户指出 CHANGELOG v0.19.0 只写了末尾 2 个 UI fix，漏掉了 v0.18.1→v0.19.0 之间约 205 个提交的全部积累（飞书修复包/八连修/二轮、ComfyUI 大修、技能库+MCP 体系、画布语义重构、时间轴体验、3D 导演台、设置集中化、营销站双语化等）。已重写 CHANGELOG v0.19.0 章节按主题归类全部范围。
- 将补全后的 CHANGELOG amend 进发布提交（916f3afa → 55f28f14），移动 v0.19.0 tag 到新提交，force-with-lease 推送 main + force 推送 tag。最终本地/远程 HEAD 均 55f28f14，v0.19.0 tag 指向它。教训：发布版本的 CHANGELOG 必须覆盖该版本实际包含的全部提交区间，不能只写本次收尾改动。
- 处理 GitHub Secret Scanning 告警：共 7 条（#1–#7，误数成 8，#8 不存在）Google API Key 告警，全部 `publicly_leaked:true`、指向孤儿提交 01e9d393（之前清胶囊那串之一，已不在任何分支/tag，含 v0.19.0）。根因：`.independent-retest/` 浏览器测试 profile 缓存（Service Worker CacheStorage）夹带了访问过的网站留下的第三方 Google key，被误提交进 01e9d393。处理：删除工作区 .independent-retest/（1.3M 垃圾）、在 .gitignore 加忽略规则（commit 81c55e7d 推送）、用 gh api PATCH 将 7 条告警全部 resolved/wont_fix。当前 main HEAD = 81c55e7d，本地门岗全量扫 2601 文件通过。注意：密钥仅在孤儿对象里，未做全量历史 purge（需重写 205+ 提交跨多分支，风险大未执行）；若用户要求零痕迹可再做。
