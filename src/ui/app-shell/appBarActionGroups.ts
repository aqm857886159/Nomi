export const APP_BAR_ACTION_GROUPS = {
  /** 剪辑面板布局（只在预览页出现）。 */
  layout: ['editingLayout'],
  assist: ['onboarding', 'browser'],
  config: ['settings', 'modelAccess'],
  primary: ['goToProduce'],
} as const
