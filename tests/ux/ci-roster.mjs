// CI 走查清单 —— 哪些走查每次相关改动都必须真跑一遍。
//
// 为什么需要这份清单（2026-09-02 用户拍板「挑关键零额度走查进 CI」）：
// 走查过去是**写完就没人跑**的资产。189 份走查里绝大多数不在任何 CI 里，唯一的执行者是
// 「某人临时想起来跑一次」。后果不是抽象的，已经发生两次：
//   · 2026-06-23：j3-onboarding / j5-edit-export 断言的 UI 被产品决策移除 → 永久假红 →
//     当时的处置是把旅程整个删掉（见 evals/journeys/index.mjs 顶部注释）。
//   · 2026-08-03：feeb575b 按设计系统 §1.5 把语言钮归位设置、并删掉 LanguageMenuButton →
//     library-language-switcher 与 multi-user-isolation 共 5 处锚点变成死选择器 →
//     红了整整一个月无人知晓，直到 2026-09-02 有人为了验别的改动偶然跑到它。
// 两次都是同一个机制：产品有意搬家，验证它的走查没跟着改，而没有任何东西会告诉你。
//
// 准入门槛（三条全中才进这份清单，缺一条都不许加）：
//   ① 零额度：全程不发真实生成/模型请求，不烧 API 额度。
//   ② 确定性：不依赖外网、不依赖用户真实档案（requireCatalog: false 或自带合成 catalog）。
//   ③ 已实测跑绿：加进来之前必须在本地真跑一遍并全绿，绝不把现在就红的走查塞进 CI
//      —— 那会立刻变成「长期红着的 CI」，比不跑更糟（人会开始无视 CI 的红）。
//   ④ 不靠手工前置：CI 上没人替它 ffmpeg 造素材、没人起本机服务。走查自己造不出前置条件
//      就不合格（2026-09-02 普查实例：asset-audio-upload 要求先手造 probe-tone-3s.mp3）。
//
// 扩充方式：满足上面四条就往 ROSTER 里加一行，`why` 写清它守的是哪个不变量。
// 不满足②（需要真实 key / 真生成）的走查**不要**放这里，它们属于付费验收，另行人工触发。
//
// ⚠️ 扩充节奏（2026-09-02 普查后定的纪律）：**逐批加，每批先在真 CI 上跑绿一轮再加下一批。**
// 那次普查把 111 个零额度候选裸跑了一遍，48 条在 macOS 上直接绿。一次把 48 条全塞进来很诱人，
// 但本仓已经栽过平台差异：canvas 性能预算在 macOS 校准、到 Linux CI 就假回归；
// GH Windows runner 会把窗口夹到下限导致布局断言全错。macOS 绿 ≠ Linux+xvfb 绿，
// 一次性塞 48 条等于赌 CI 不会大面积翻红 —— 真翻了就正好触发准入门槛③要防的那件事。
// 普查明细（48 绿 / 63 红的分类与候选池）见 docs/audit/2026-09-02-walkthrough-health-census.md。
export const ROSTER = [
  {
    file: 'first-launch-system-locale.walk.mjs',
    why: '首启按系统语言探测（#1a）。它是「用户第一眼看到什么语言」的唯一自动化证据。',
  },
  {
    file: 'library-language-switcher.walk.mjs',
    why: '不必先建项目就能改语言（#1b 的意图，入口已按 §1.5 归位设置）。'
      + ' feeb575b 声称「可达性零损失」，这条走查就是那句声明的持续证据。',
  },
  {
    file: 'i18n-sweep.walk.mjs',
    why: '双语全扫（51 项）。i18n 一期后文案面最广的一道网。'
      + ' 2026-09-02 它首次上 CI 就抓到本地测不出的东西，是这套 roster 的第一个战果。',
  },
  {
    file: 'multi-user-isolation.walk.mjs',
    why: '同机两实例并发：项目不串台 + 语言设置不串台。sequential 旅程覆盖不到这个角度。',
  },
  {
    file: 'workbench-en-overview.walk.mjs',
    why: '英文下主工作台全览。中文能跑通不代表英文布局不炸（长文案挤破容器是常见坑）。',
  },
]

// ── 已闭环：i18n-sweep 的那一轮（留着，因为它是 P6 最好的实例）──────────────
// 2026-09-02 它首次上 CI 就红：英文界面提示词库有 20 处中文（表情预设 / Sora 官方 /
// 喜悦 1/5 · 一丝笑意）。而我在 macOS 上跑了三次都是 51/51 —— 截图一看，本地面板停在
// 「Fetching prompts from the public library…」，内容压根没加载出来，
// 于是「无残留中文」这条不存在型断言恒真（P6 说的那种假绿，它当时没有基线）。
//
// 两处都修了，缺一都不算完：
//   ① 豁免：那些字来自远端公共提示词库的策展内容（用户 2026-09-02 拍板不翻译）。
//      标记 data-remote-content 打在渲染它们的那两层（PromptCard 的标题/来源块、
//      非「全部来源」的来源 chip），**不是**整个面板——面板自己的 UI 文案必须继续被抓。
//   ② 基线：断言前先等 loading 指示散场，仍在转就报红。光做①不做②，
//      下次换个更慢的面板照样假绿——治的是症状不是那一类。
// 阳性对照：摘掉豁免后本地也抓到 18 处（CI 是 20，差在虚拟列表渲染行数），
// 证明面板真渲染了内容、豁免确实在起作用，而不是又一次「没加载出来所以没看到」。
