{
  "version": 1,
  "screen": "agent-panel",
  "calibratedPlatform": "darwin",
  "viewport": { "width": 1440, "height": 1000, "deviceScaleFactor": 1 },
  "colorScheme": "light",
  "tolerance": {
    "threshold": 0.2,
    "maxDiffPixelRatio": 0.002
  },
  "why": {
    "calibratedPlatform": "基线是在 macOS 上渲染并由用户拍板的。字体栅格化在 macOS 与 Linux(xvfb 软渲染) 上本来就不一样，把 darwin 的 PNG 拿到 ubuntu runner 上逐像素比 = 必红，而且红的是平台不是设计（同 canvas 性能预算那次的教训：docs/lessons 里 canvas-perf-budget-calibrated-on-macos-fails-on-linux）。所以视觉比对只在这里声明的平台上执行；别的平台跑结构检查并明说「视觉道在本平台未校准」，不假装绿也不假装红。要加一个平台，就在那个平台上重录一套基线、把它加进 calibratedPlatform（届时改成数组）。",
    "colorScheme": "App 默认是「天黑自动暗」（按本地时间翻 token）。基线要是跟着钟走，同一份代码上午绿晚上红。实验室因此永远显式钉死浅色；暗色是手动看的一道（?scheme=dark），不进基线。",
    "viewport": "1440×1000 @1x：面板本身固定 340px 宽（workbenchStore.assistantWidth 默认值），视口只需要装得下最高的那一格（620px 舞台 + 说明行）。deviceScaleFactor 固定 1，避免不同机器的 DPR 把基线换算成两倍尺寸。",
    "threshold": "0.2 = Playwright 默认的 YIQ 感知色差容差。不放宽：一个 token 变了（哪怕只是 ink-60 → ink-40）色差远超它，必须红。",
    "maxDiffPixelRatio": "0.002（千分之二）。只为吸收同平台同浏览器下字形边缘的亚像素抖动——一格 340×620 ≈ 21 万像素，千分之二 ≈ 420 像素，约等于几个字的边缘。任何真实设计改动（间距、圆角、色板、字号、增删一个控件）动的都是成块的像素，量级在千像素以上，越不过这道线。R17 红证：改一个 token 跑一次，必须红。"
  }
}
