# Nomi 能力系统当前基线

> 生成时间：2026-08-28T21:45:52.855Z
> 基线类型：真实 Electron 入口 + 当前代码合同就绪度，不是模型质量评测。

当前结果：0 success / 10 partial / 0 blocked；P0 就绪度 25%。

本机没有可用文本模型，因此 `agentTurns`、token、真实完成时间和输出质量均保留为 `null`。这不是跳过失败：当前基线明确证明了入口和合同缺口；模型接好后必须用同一任务定义复跑 with/without。

| 任务 | 状态 | 当前可用面 | 关键缺口 |
|---|---|---|---|
| J1 一句产品 brief 变成 30 秒竖屏规格 | partial | 有相关入口 | 缺少 CreativeBrief、AgentElicitation |
| J2 研究近 30 天某视觉趋势 | partial | 有相关入口 | 缺少 ResearchEvidence |
| J3 拆解一条参考视频 | partial | 有相关入口 | 缺少 ShotBreakdown、MediaDigest |
| J4 为三个镜头找 B-roll | partial | 有相关入口 | 缺少 AssetSourceEvidence、Pexels、Openverse、Freesound |
| J5 把一条镜头规格编译给两个视频模型 | partial | 有相关入口 | 缺少 PromptRecipe、PromptCompiler |
| J6 给 60 秒片做声音设计 | partial | 有相关入口 | 缺少 SoundCue、Freesound |
| J7 用文本模型继续讨论之前的媒体 | partial | 有相关入口 | 缺少 MediaDigest |
| J8 审片并修三个连续性问题 | partial | 有相关入口 | 缺少 QualityReport |
| J9 导出商业片 | partial | 有相关入口 | 缺少 AssetSourceEvidence、RightsCheckReport |
| J10 三个月后打开旧项目 | partial | 有相关入口 | 缺少 CapabilityLock、PromptRecipe |

## 环境证据

- Electron 启动：成功；隔离项目创建：成功；探测耗时：4327 ms。
- 已实现相关 surface：Prompt 选择器=true，浏览器=true，素材库=true。
- 空白项目当前 DOM：Prompt 选择器=false，浏览器=true，素材库=false；未挂载不等于未实现。
- 当前缺失入口：结构化补问=true，权利筛选=true，能力库=true。
- 模型目录：0 个已启用文本模型。

## 复跑门

1. P0 实现后先复跑 J1-J4/J9/J10，要求结构化产物真实写入项目。
2. 配置文本模型后，补 `agentTurns`、token、time-to-outcome 和 with/without 质量分。
3. P0 通过后再跑 J5-J8；P1 能力不能用来掩盖 P0 合同缺失。
